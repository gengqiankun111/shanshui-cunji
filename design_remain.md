# design_remain.md —— 设计未落地 / 留待设计点

> 2026-09-03 治理：**已完成设计与评估定档**内容已回填 design.md（§23 设计决策归档）与
> development.md（§14 收口归档 / §13 队列 / 7.x）；**明确不开发** → design_givenup.md；
> 本文件仅保留 **远期蓝图 / 条件触发 / 待用户确认 / 部署硬件验收** 的设计点。
>
> **分工（与 development_remain.md 统一口径）**：本文件 = 设计蓝图 / 触发条件 / 验收基准详述；
> development_remain = 开发排期唯一跟踪（同一主题的执行状态、commit、验收进度以 development_remain 为准）。

## 一、远期蓝图（触发条件满足才落地）

### 1. Calvin 全局事务序（design_extension 13.3/14.8/14.9 远期）

- **状态**：🔍 远期，当前不进入 kernel（Ex-3 评估完成，见 design_givenup.md）
- **触发条件（13.3.1）**：强一致多 docid 跨分区事务需求 + 读写集可静态预声明
- **落地阶段（14.8）**：① 全局 gseq 分配器（持久化游标不重号）+ 单写者确定性执行 demo →
  ② 全局复制日志 + 分区确定性执行 + 幂等键 `(gseq, docid)` + 落后追赶 →
  ③ 序节点高可用（raft 复制 gseq 游标与日志）+ 故障转移 + 对账
- **API 形态（14.9）**：读写集声明 `POST /txn/submit`；单 docid 不走全局序；交互式先读后写不支持

### 2. 存算分离 / Indexer Node（彻底化）

- 查询代理层已落地（IndexerProxy，7.79）；**彻底存算分离**触发 = 百亿级规模（50 亿属过度设计）
- 相关远期：Roaring64（docid 20 亿+）、Calvin gseq raft 联动（依赖 Calvin）

### 3. Tiered 分层合并

- 7.78 模拟验证：写放大不优于 Leveled + 当前分批/无锁合并已解决阻塞 → **暂不引入**；
  触发 = 写放大再优化需求（key 级更新密集场景）复评

### 4. Ex-8.4：L1/L2 B+Tree 存储替换（备选蓝图，不主动排期）

- 触发：Ex-8.1~8.3 落地后范围仍未达标 + 读多写少极端场景（当前 50m 已 3.6× MySQL，主路径收敛）

### 5. 共享字典压缩（ScyllaDB 式）

- 触发：Ex-8.12（无字典 zstd 高等级）后空间仍瓶颈且压缩率需求明确再评估

### 6. 写路径多写者 / 无锁写侧（Ex-13 触发链）

- 无锁多写者 + 每写者独立 WAL + NVMe 多队列/多设备；前置 = 解除引擎写侧串行 + 吞吐实测指向写锁

## 二、待用户确认 / 澄清

### 存储格式现状 vs 设计 3.4（§16，2026-09-03）

- 差异已核实记录：现状 ≈"行式字段列集 + JSON 原文值"，非 design 3.4 的字段ID紧凑文档二进制
- **待定**：是否落地字段ID紧凑编码（P3 远期、需排期+迁移验证）或**修订 design 3.4 描述**消除偏差 ——
  改设计文档需用户确认（feature_remain 同步跟踪）

## 三、部署 / 硬件验收（机制已落地；开发跟踪 → development_remain 二）

| 项 | 状态 | 说明 |
|---|---|---|
| 10 分片 10 亿构建验收 | 机制 A~D 全落地（7.81~7.98，550 全绿）；开发跟踪 → development_remain 二 | 硬件验收：构建 ≤60 分钟、亿级 posting 规模回归、点查 ≥10 万 QPS / 写 ≥20 万 rows/s 真机、真实 TCP 扩容·故障切换联调（`tmp_10b_acceptance.py` 就绪） |
| 高并发查询吞吐达成 | 功能完成（2802885 + 本机 10k 连接复测达成） | 85 万 QPS 需 16 核/64G/NVMe 目标硬件复测（本机物理核仅目标 37.5%，上限 ≈37.5k） |
| 读写分离再启 | 暂缓（组提交已解决；等价能力落地） | 复制型分布式阶段（读放大场景）再启；跟踪见 development_remain 三（写路径多写者链）与 feature_remain ⏸ |

## 设计决策边界（已定，不重复评估；正式归档见 design.md §23）

- 桶 / 分区 LSM（固定哈希分桶 + 桶内重叠度调度 Compaction）：不立项
- 2PC / TCC / Seata 本体：不做（L1 outbox + L2 SAGA 已覆盖）
- SAGA vs Calvin：SAGA 是当前与近期的答案；Calvin 远期触发
- 读写分离：暂缓（组提交已解决）
- 无锁合并：根治已落地（P72/P73，development 7.65）
