架构选型文档
项目名称：novosdb
文档版本：v1.0
编制日期：2026-08-26
文档状态：定稿

1. 选型背景与目标
novosdb 的核心工作负载特征：

特征	具体描述
写入模式	高吞吐持续写入（日志、埋点、IoT 上报），随机 DocId，单条或小批量
查询模式	热点主键点查（绝大多数）、带条件的倒排检索（运营/分析场景）
数据规模	单节点设计基准 1.5 亿文档（300GB 磁盘），集群线性扩展至 50 亿级
数据模型	文档型（DocId → 结构化字段集合），支持任意字段筛选
一致性要求	最终一致性可接受（边缘网关场景），不强求 ACID 事务
2. 核心架构决策
2.1 存储引擎：LSM-Tree vs B-Tree
这是数据库架构最根本的抉择。

对比维度	B-Tree（如 MySQL InnoDB、SQLite）	LSM-Tree（如 RocksDB、LevelDB、Cassandra）
写入性能	中等，随机写导致页分裂，写放大 5~10 倍	优秀，顺序写 + 批量刷盘，专为高吞吐写入优化
读取性能（点查）	优秀，3~5 次磁盘 I/O 可定位	良好，需查 MemTable + 多层 SST + 布隆过滤
读取性能（范围扫描）	优秀，叶子节点链表顺序扫描	良好，SST 有序但可能跨多个文件
写入放大	5~10 倍	10~30 倍（Compaction），可通过调优降低
空间放大	较低（页内碎片）	较高（旧版本数据等待 Compaction 回收）
并发控制	行锁 + MVCC（成熟）	需要自行实现（或依赖 MVCC 层）
崩溃恢复	WAL + Redo Log	WAL + 重放 MemTable
决策依据：

novosdb 的核心场景是日志、埋点、IoT 上报——写入吞吐是第一优先级。B-Tree 的随机写瓶颈在每秒数万 TPS 时就会暴露，而 LSM-Tree 专为高吞吐写入设计。

虽然 LSM-Tree 存在读放大和 Compaction 写放大，但通过以下设计可有效控制：

HotCache：热点主键查询完全绕过 LSM，亚毫秒返回

布隆过滤器：快速跳过不存在的 key

块缓存 + Zone Map：减少读放大

Leveled Compaction + IO 优先级隔离：控制 Compaction 对前台的影响

✅ 决策：LSM-Tree

2.2 索引架构：倒排索引的实现路径
novosdb 需要支持任意字段自由 AND/OR 筛选，这要求倒排索引具备：

需求	技术要求
Term → DocId 列表查找	O(1) 或 O(log N)，支持千万级 Term
DocId 列表存储	高压缩率（RoaringBitmap），支持快速交集/并集
写入时更新	追加友好，避免随机写
内存可控	64GB 机器下倒排字典 < 12GB
方案对比：

方案	优点	缺点	适用性
倒排查入 LSM KV（RocksDB 风格）	实现简单，复用 LSM 基础设施	Term 查找需多层二分（5~7 次磁盘 IO），延迟 5~30ms	❌ 不满足延迟目标
内存哈希字典 + 追加倒排文件（本方案）	Term 查找 O(1)，写入追加（顺序 IO），延迟 <1.5ms	需独立实现 GC 和段管理	✅ 采用
FST + Mmap（Lucene 风格）	冷启动亚秒级，RSS 按需加载	实现复杂，FST 不可变需分层	✅ 阶段 1.5 升级
✅ 决策：MVP 采用「内存哈希字典 + 追加式倒排文件」，阶段 1.5 升级 FST + Mmap【design.md 5.2.4.1】。

2.3 缓存架构：多级缓存策略
缓存层级	用途	淘汰策略	内存占比（64GB 示例）
HotCache	热点文档（完整对象）	LFU（默认）/ LRU / Tiny-LFU	8GB（12.5%）
BlockCache	SST 数据块	LRU	4GB（6.25%）
TermCache	倒排 Term 的 Top N DocId	按访问热度淘汰	2GB（3.1%）
Page Cache	OS 级文件缓存	LRU（内核管理）	预留 8~16GB
设计原则：

HotCache 是加速副本，非权威数据源，崩溃后可穿透磁盘重建【design.md 6.1】

三层缓存硬隔离，防止相互挤占导致命中率雪崩【design.md 6.5】

淘汰策略可配置，适应不同工作负载

2.4 分布式架构：分片策略
方案	优点	缺点	适用性
Hash 分片（DocId % N）	实现简单，路由确定，单点查询极快	扩容需 rehash 全量数据	❌ 扩容困难
一致性 Hash（虚拟分片）	扩容只迁移部分数据，平滑	实现稍复杂	✅ 采用
范围分片（按 DocId 区间）	范围查询友好	热点倾斜严重	❌ 不适合随机 DocId
✅ 决策：一致性 Hash + 1024 虚拟分片【design.md 9.1】，扩容时只迁移部分虚拟分片，业务不停机。

2.5 复制与高可用
方案	写入延迟	数据安全性	适用性
异步复制（本方案默认）	≈ 单机（0.7~1.8ms）	极端宕机可能丢少量最新数据	✅ 边缘网关（吞吐优先）
同步复制（Quorum）	+1~3ms（网络 RTT）	强一致，不丢数据	⚠️ 金融等强一致场景
Raft 三副本	+3~8ms	强一致 + 自动选主	❌ 违背“放弃强一致换速度”原则
✅ 决策：默认异步复制【design.md 9.3】，同步模式作为可选项（replication.sync_mode = "sync"）。

3. 完整架构蓝图
text
┌─────────────────────────────────────────────────────────────────────┐
│                        网络服务层                                   │
│         HTTP-JSON / TCP 简易协议 / CLI / 类 SQL WHERE              │
│                    （基于 sqlparser-rs 受限子集）                   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                     查询层（Query Layer）                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              查询优化器 Lite（多级索引路由）                  │   │
│  │  主键点查 → 组合索引 → 倒排 + Zone Map → 全表扫描（降级）    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              聚合执行器（COUNT / GROUP BY）                   │   │
│  │       直接读内存 TermMeta 的 doc_count，零回表               │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                      缓存层（Cache Layer）                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │  HotCache    │  │  BlockCache  │  │     TermCache            │ │
│  │  (LFU/LRU)   │  │   (LRU)      │  │  (Top N 片段)            │ │
│  │  文档完整对象 │  │  SST 数据块  │  │  倒排热门 Term 前缀      │ │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                     索引层（Index Layer）                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              列族隔离（Column Family）                       │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │ cf_data  │  │ cf_cidx  │  │ cf_inv   │  │ cf_delta │   │   │
│  │  │ 主数据    │  │ 组合索引  │  │ 倒排索引  │  │ 增量更新  │   │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              倒排索引：内存字典 + Append 文件                │   │
│  │     DashMap<Term, TermMeta> → 段清单 → .inv 文件            │   │
│  │              RoaringBitmap 压缩 DocId 列表                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                   LSM-Tree 存储引擎（Storage Engine）               │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  WAL（组提交 / 双模式 / 循环分段 / 延迟删除）                │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  MemTable（跳表 / 双缓冲切换 / 写 Stall 限流）               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  SSTable（PAX 混合列组 / 前缀压缩 / Zone Map / 布隆过滤器）  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Compaction（Leveled / IO 优先级隔离 / 时间分区 TTL）        │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                   稳定性与运维（Stability & Ops）                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ OOM Guardian │  │Write Stall   │  │  Query SLA Guard         │ │
│  │ （内存限流）  │  │ Watchdog     │  │  （超时/并发熔断）       │ │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘ │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ Sidecar 探针 │  │ 冷备份/还原  │  │  配置热加载（SIGHUP）     │ │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                   分布式层（Distributed Layer，阶段 2）             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ Proxy-Gateway│  │ Meta-Server  │  │  TDS（术语字典缓存服务）  │ │
│  │ （无状态路由）│  │ （分片路由表）│  │  （字典快照预推送）      │ │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  分片节点（复用单机内核）│ 虚拟分片平滑扩容│ 一主多从异步复制  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
4. 架构决策汇总
决策点	选择	核心理由
存储引擎	LSM-Tree	高吞吐写入是首要目标
索引架构	内存哈希字典 + 追加倒排文件	Term 查找 O(1)，写入顺序 IO，延迟 <1.5ms
缓存策略	三层隔离（HotCache + BlockCache + TermCache）	防止相互挤占，各自独立调优
分布式分片	一致性 Hash + 1024 虚拟分片	扩容平滑，业务不停机
复制模式	异步复制（默认）+ 同步可选	吞吐优先，强一致作为可选项
并发模型	tokio 多线程 + 同步内核	兼顾异步 I/O 效率和并发安全
部署形态	单机先行 → 分布式演进	先跑通核心链路，再水平扩展
5. 与竞品架构的定位差异
对比项	RocksDB	TiKV	Badger	novosdb（本方案）
语言	C++	Rust	Go	Rust
存储引擎	LSM-Tree	LSM-Tree（Rust 重写）	LSM-Tree + Value Log	LSM-Tree + PAX 列组
索引	主键 KV	主键 KV + 二级索引	主键 KV	主键 + 组合索引 + 倒排索引
数据模型	Key-Value	Key-Value	Key-Value	文档型（结构化字段）
倒排索引	无（需上层实现）	无（需上层实现）	无	原生支持，内存字典 O(1)
热点缓存	BlockCache	BlockCache	无	HotCache（文档级）+ BlockCache + TermCache
分布式	无（嵌入式）	Raft 分布式	无（嵌入式）	网关 + 一致性 Hash 分片 + 异步复制
典型场景	嵌入式 KV 引擎	分布式 KV	Go 服务 KV	文档检索 + 日志/埋点/IoT
差异化定位：novosdb 不是另一个 KV 存储引擎，而是面向文档型数据的检索数据库——在 LSM-Tree 的高吞吐写入基础上，原生集成了倒排索引、组合索引、文档级缓存和聚合能力，填补了“RocksDB 能力强但无倒排”与“Elasticsearch 功能全但写入吞吐低”之间的市场空白。

6. 架构演进路线
阶段	范围	核心架构特征
阶段 1（MVP）	单机	LSM-Tree + 列族 + 倒排基础 + HotCache + Zone Map + 看门狗子集
阶段 1.5	单机增强	PAX 列组 + TTL 时间分区 + Delta CF + 倒排统计载荷 + FST+Mmap + 迁移工具基础版
阶段 2	分布式	倒排架构升级（预分片 Chunk + 分层 GC）+ 网关 + 一致性 Hash 分片 + 异步复制 + 全局 Term 缓存 + TDS + 看门狗补全
阶段 3	深度优化	io_uring + 环形 WAL + Leveled Compaction + 热加载 + 迁移工具
7. 参考依据
参考项	说明
B-Tree vs LSM-Tree 权衡分析	LSM 写优化，B-Tree 读优化
RocksDB/LevelDB 架构	LSM-Tree 工业级标杆
Badger（WiscKey）	Key/Value 分离降低写放大
TiKV Rust 重写	内存安全 + 20%~40% 性能提升
PAX 混合列式存储	宽表场景读放大降低 90%
RoaringBitmap	倒排列表压缩行业标准