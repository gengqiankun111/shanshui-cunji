# v0.7.0 1 亿库构建记录（本机 Windows，标准 MySQL 语句）

2026-08-31 本机完成 1 亿行数据库构建，延续 5000 万库方式（sysbench 标准 MySQL 语句）。

## 环境
- 数据库：本机 Windows（SAMSUNG MZVLB512 NVMe），shanshui-cunji-mysql-server v0.7.0
- 客户端：**Windows 原生 sysbench 1.0.20（MSYS2 MINGW64 编译）**，本机直连 127.0.0.1:3308（无 RTT）
- 配置：config.mysql-build-100m.toml（组提交 2ms，倒排排除 k/c/pad）
- 数据目录：D:/shanshui-data/db-100m-v070（新库，保留 db-50m-v070）
- 服务端口：3308（3307 保留给 50m 库）

## 构建
- 方式：sysbench oltp_point_select prepare，--table-size=100000000 --threads=8
- 表：sbtest1(id auto, k int, c varchar, pad varchar) 1 亿行 + 二级索引
- 结果：**25.6 分钟完成，EXIT=0**（建表 + 1 亿行插入 + 二级索引）

## 构建进度采样
| 时间(min) | SST 数 | 库大小(GB) | 备注 |
|---|---|---|---|
| 2.0 | 8 | 1.26 | ~65k rows/s 稳定 |
| 4.5 | 16 | 2.36 | |
| 7.5 | 25 | 3.62 | compaction 介入速率略降 |
| 10.8 | 36 | 4.96 | |
| 14.2 | 46 | 6.40 | |
| 17.5 | 56 | 7.93 | |
| 20.0 | 66 | 9.22 | |
| 22.5 | 78 | 10.80 | 数据插完，进入收尾 |
| 25.6 | - | - | 完成（含建索引） |

**结论**：本机 Windows 原生 sysbench（无 RTT + 组提交 2ms）构建 1 亿行 ≈ 6.5 万行/s，25.6 分钟；数据 ~10.8GB。

## 查询验证（1 亿行规模）
- oltp_point_select run（8 线程 15s）：**6365.8 TPS**，avg 1.25ms，95th 1.58ms，max 314ms（冷启动）
- EXIT=0

## sysbench 全套测试（1 亿库，8 线程 15-20s，mean=avg / max 延迟 ms）
2026-08-31 本机 Windows 原生 sysbench 连 3308，11 项 oltp 测试全跑通：

| 测试项 | TPS | mean(ms) | max(ms) | 说明 |
|---|---|---|---|---|
| oltp_point_select | 5,087 | 1.57 | 308.6 | 单条点查（事务外） |
| select_random_points | 6,316 | 1.27 | 12.4 | 多点随机查 |
| select_random_ranges | 7,072 | 1.13 | 312.7 | 随机范围查 |
| oltp_insert | 12,958 | 0.62 | 32.6 | 纯插入（组提交摊薄） |
| oltp_delete | 16,239 | 0.49 | 28.5 | 纯删除（删除位图） |
| oltp_update_index | 13,255 | 0.60 | 35.1 | UPDATE k=k+1（字段自增） |
| oltp_update_non_index | 9,539 | 0.84 | 65.7 | UPDATE c='str'（字符串赋值） |
| bulk_insert | 321,803 | 0.02 | 991.2 | 批量多行 INSERT（sbtest7/8） |
| oltp_read_only | 42.2 | 189.3 | 532.5 | 事务内 14 条语句（BETWEEN/SUM 逐 id 扫描） |
| oltp_write_only | 296.6 | 27.0 | 240.2 | 事务内 UPDATE/DELETE/INSERT（uniform） |
| oltp_read_write | 29.5 | 269.9 | 3,706 | 事务混合（uniform；并发写冲突 1213 跳过） |

**要点**：
- 单语句读写（点查/插入/删除/更新）TPS 0.5-32 万，延迟 sub-ms 级；**事务类（read_only/read_write）TPS 低**——事务内多语句 + RR 快照读每语句独立走 txn_get，且 BETWEEN/SUM 范围查询逐 id 扫描 + 并发写冲突（sysbench 8 线程同时间种子生成同 docid 序列）导致大量事务被 1213 跳过。
- sysbench 默认 `--rand-type=special` 会放大同 docid 冲突；写类测试改用 uniform。
- 冲突处理：TxnConflict/TxnDeadlock 映射 MySQL 1213（ER_LOCK_DEADLOCK，sysbench 默认忽略跳过），不再 FATAL。

## P0 优化复测（M 项范围扫描一次化 + N 项 batch_get，commit d044b4c）
2026-08-31 新 binary 重启 3308 后重测事务类（8 线程 20s）：

| 测试项（rand） | 旧 binary TPS / mean | 新 binary TPS / mean | 提升 |
|---|---|---|---|
| oltp_read_only (special) | 42.2 / 189.3ms | **70.9 / 112.7ms** | **+68%** |
| oltp_read_write (special) | 29.5 / 269.9ms | **53.0 / 150.7ms** | **+80%** |
| oltp_read_only (uniform) | - | 68.0 / 117.5ms | |
| oltp_read_write (uniform) | - | 58.0 / 137.6ms | |

**结论与瓶颈诊断**：
- M 项（BETWEEN/SUM 从逐 id 展开改为 `scan_range_txn` 一次快照扫描）生效：事务类 +68%~80%，读延迟 -40%。
- **未达预估 ~50×**。单连接计时实测（3308，10 条语句/连接）：点查与 100 行范围 SUM 均亚毫秒级（范围 0.2ms/条）——**scan_range_txn 本身不慢**。
- 真实瓶颈 = **引擎全局单 Mutex 每语句串行**（mysql.rs `Arc<Mutex<Engine>>`，每条 COM_QUERY 持锁执行 ~1ms）。8 线程 × 14 语句/事务排队 → 服务器吞吐天花板 ≈ 1000 stmt/s（70 TPS × 14 语句 = 980 stmt/s，与点查 5k TPS = 0.2ms/语句的串行上限吻合）。
- **后续 P1 方向**：① 读路径 `RwLock` 化（RR 快照读只读，读读并行，需读 API 化 &self）；② MySQL 层事务内语句批处理（BEGIN..COMMIT 合并 dispatch，省 13 次锁往返）；③ 快照读走 HotCache（get_at 可加 per-txn 小缓存，不污染全局热缓存）。

## P 项事件驱动自动 Compaction 上线验证（2026-08-31，commit 24861c6）
- **背景**：1 亿库构建期无自动合并，78 个 L0 段全部留在读路径（点查逐段布隆+索引定位）。
- **验证**：新 binary 重启后，单条 INSERT 触发写路径 `auto_compact` —— 78 个 L0 段**同步合并收敛为 1 个 L1 段**（~10.8GB，合并耗时约 5-6 分钟，期间写入阻塞 = 背压语义）；INSERT 完成后数据完整可读（id=100000001 回查命中）。
- **效果**：oltp_read_only（special，8 线程 20s）：
  | 阶段 | TPS / mean | 对比基线 |
  |---|---|---|
  | 基线（旧代码，78 L0） | 42.2 / 189ms | - |
  | M 项后（78 L0） | 70.9 / 113ms | +68% |
  | **P 项合并后（1 L1 段）** | **75.8 / 105ms** | **+80%** |
- **结论**：点查从 78 段遍历降到 1 段仅 +7%——印证事务类瓶颈是**引擎全局 Mutex 串行**（O 项，~1000 stmt/s 天花板）而非 SST 遍历；SST 收敛的主要价值是文件数/读放大可控（防 78 段无限堆积）。

## 新增能力：SET TRANSACTION ISOLATION LEVEL（2026-08-31）
- mysql.rs 支持 `SET [SESSION] TRANSACTION ISOLATION LEVEL READ UNCOMMITTED|READ COMMITTED|REPEATABLE READ|SERIALIZABLE`（会话级，BEGIN 时生效）
- 默认 REPEATABLE READ；READ UNCOMMITTED 映射 READ COMMITTED（无脏读语义）
- 端到端验证（3308）：4 级别 SET 全 OK；SERIALIZABLE 事务点查 4128 qps / mean 0.242ms
- 单元测试：parse_isolation_level_variants（mysql 测试 13 项全绿）

## 对照
- 5000 万库（阿里云客户端 RTT 52ms）：2000-4800 行/s（构建）
- 本机 qemu2（无 RTT）：~52k 行/s（构建）
- 本机 Windows 原生 sysbench：~65k 行/s（构建），1 亿行点查 6366 TPS
