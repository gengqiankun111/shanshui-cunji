# 10 亿库扩展方案（design-10b-extension.md）

> 2026-09-02 · 基于当前架构（v0.8.0+，500 全绿）规划从 1 亿库扩展到 10 亿库的具体方案。
> 核心思路：**分片横向扩展**（N 分片 × 单机能力线性扩展）——当前分片基建 + 倒排优化 +
> 扩容编排已覆盖 10 亿库 70%+ 需求，核心缺口 = **全局 docid 分配器** 与 **10 亿规模实测验证**。

## 1. 背景与目标

- 当前 1 亿库（db-100m-v070）：点查 26.5k TPS、事务 read_only 526 TPS、写入 26.5k rows/s、
  数据 ~10.8GB（primary SST）；单机读路径已无锁化（RwLock + ArcSwap + Zone Map）。
- 目标：**10 亿库**（10×数据量）——点查 10 万+ QPS、写入 20 万+ rows/s（分片并行）、
  构建 ~30 分钟（分片并行）、支持在线扩容与故障自动切换。
- 约束：docid 上限（Roaring 32-bit = 42.9 亿，10 亿安全）；跨分片聚合（广播检索 + Chunk 拼接）；
  跨分片事务（单 docid 本地事务 + SAGA，维持现有决策）。

## 2. 规模画像（估算）

| 维度 | 1 亿库实测 | 10 亿库估算 |
|---|---|---|
| 主数据（primary SST） | ~10.8GB | ~108GB（+ 多副本/倒排/增量 ≈ 300GB 级） |
| docid | 1.5 亿（seq 峰值） | 10 亿——**< u32::MAX（42.9 亿）**，Roaring 32-bit 可用，余量 3.3× |
| 高频 posting | ~1600 万 | 亿级（v3 分块 + LRU 缓存 + 后台 GC 已优化） |
| 点查吞吐 | 26.5k TPS（单机 8 线程） | 目标 10 万+ QPS（分片并行） |
| 写入吞吐 | 26.5k rows/s（单机） | 目标 20 万+ rows/s（分片并行） |
| 构建时间 | 25.6 分钟 | 单机 ~4.3 小时；分片并行 ≈ 26 分钟 |

## 3. 现状能力盘点（已具备，10 亿库基建）

| 组件 | 状态 | 用途（10 亿库） |
|---|---|---|
| Gateway 分片路由 | ✅ | `hash64(docid) % vshards` 一致性哈希写路由 + 广播检索 Chunk 拼接（design 9.1/9.2/5.2.1） |
| MetaCenter | ✅ | 虚拟分片路由（默认 1024）、扩容重路由（~25% 已验证）、节点注册/摘除 |
| 分片节点 | ✅ | 每节点独立 Engine（primary/cidx/delta CF + 本地倒排 + 删除位图 + HotCache） |
| 主从复制 | ✅ | `repl.apply` 幂等应用（async 攒批 / sync 立即 ACK） |
| 扩容编排 | ✅ 7.76 | `scale_out.rs`：ADDING→CATCH_UP→DRAIN→SWITCH→DONE + 回滚预案 + 崩溃续跑 |
| 元数据 raft | ✅ 7.77 | `raft_meta.rs`：master 自动 failover + 脑裂安全（RPC 接线为阶段 C） |
| 倒排全套 | ✅ | posting 分块（K 7.74）、段 GC 后台化（J 7.73）、LRU 缓存 + mmap（G/Ex-5.7） |
| 写路径 | ✅ | 组提交（M8）+ 无锁合并（P72）+ 批量导入模式（60k rows/s） |
| 读路径 | ✅ | 读读并行（O）+ Zone Map 粗筛（R）+ 4KB 预读（U） |

## 4. 瓶颈分析与设计决策

| 瓶颈 | 分析 | 决策 |
|---|---|---|
| 容量 | 单机 108GB 可存（SSD 2TB+），但倒排/副本/增量放大 → 单机不可持续 | **分片横向扩展**（主路径） |
| docid 上限 | 10 亿 < 42.9 亿（u32）安全；20 亿+ 接近边界 | 分片前缀 docid 预留低位空间；超限时 Roaring64 改造（远期） |
| 单机吞吐 | 读无锁化已达 CPU/IO 上限（26.5k 点查） | 分片并行（4~10 分片线性扩展） |
| 全局 docid 唯一 | 单机 auto_id 不适用多分片；一致性哈希依赖 docid 分布 | **新增全局 docid 分配器**（分片前缀方案） |
| 构建时间 | 单机 10 亿 ~4.3 小时 | 分片并行导入（复用 import + 分片路由） |
| 倒排规模 | 亿级 posting + 广播检索跨分片 | 分片本地倒排 + Chunk 拼接（已具备），10 亿验证 |

## 5. 分片架构设计（含全局 docid 分配器）

```
客户端 / MySQL 协议
        ↓
  Gateway（现有）：写路由 / 广播检索 Chunk 拼接
        ↓
  MetaCenter 路由 + 全局 docid 分配器（新增）
        ↓
  Shard-0 ─ Shard-1 ─ … ─ Shard-9      （10 分片 × 1 亿 = 10 亿库）
  每分片 = Engine（WAL/MemTable/SST + 本地倒排 + 删除位图）+ Slave 副本
```

### 5.1 全局 docid 分配器（分片前缀方案，阶段 A 核心）

- **格式**：`docid = shard_id << 40 | local_id`——高 N 位 = 分片号，低 40 位 = 分片内自增；
- **优点**：
  - 路由 **O(1)**（高 N 位直取分片，无需 `hash64(docid)` 重映射——扩容时 docid 归属不变）；
  - 分片内自增（`AtomicU64`，无集中分配瓶颈，gseq demo 验证 1-2 亿/s 余量充足）；
  - 低 40 位 = 1 万亿容量/分片，10 亿库远超余量；
- **兼容**：现有 `hash64(docid)` 路由仍可用（哈希分布到分片）；分片前缀为**推荐新写路径**
  （构建/批量导入），渐进切换（测试 + 文档 + 兼容 hash 路由路径）；
- **实现**：新增 `src/docid_alloc.rs`（分片前缀编码 + 分片内分配器 + 路由解析），
  与 MetaCenter 分片数联动（扩容新增分片号段）。

### 5.2 分片数规划公式

```
分片数 = max( 容量: ceil(108GB × 放大系数 / 单节点目标容量) ,
              吞吐: ceil(目标 QPS / 单机 26.5k) )
```
- 目标 10 万 QPS + 300GB 放大 → **4~10 分片**（推荐 10 分片 × 1 亿，粒度对齐已验证规模）。

### 5.3 构建策略

- 10 分片并行导入（每分片 1 亿，复用 sysbench/import + 分片路由 docid 前缀）；
- 预计 ~26 分钟（10 分片 × 1 亿 × 6.5 万 rows/s / 10 并行 ≈ 25.6 分钟）。

## 6. 分阶段方案

| 阶段 | 内容 | 交付 | 状态 |
|---|---|---|---|
| **A** | 分片横向扩展：全局 docid 分配器 + 分片构建工具 + 10 分片 10 亿构建 | docid_alloc 模块 + 构建脚本 + 10 亿库 | 待开发 |
| **B** | 倒排分片化验证：10 亿 posting 规模回归（广播检索 + Chunk 拼接 + 分块解码） | 10 亿库倒排查询基准 | 待验证 |
| **C** | 扩容自动化衔接：scale_out + raft_meta 接 RPC（raft 阶段二） | 真实 10 节点扩容/故障切换联调 | 待开发 |
| **D** | 可观测与运维：分片级 Metrics + docid 上限预警 | 监控面板 + 告警 | 待开发 |

## 7. 需要的新开发项（按序）

1. **全局 docid 分配器**（`src/docid_alloc.rs`，阶段 A 核心）——分片前缀编码 + 分片内分配 +
   路由解析；demo（`src/demo/docid-alloc`）+ 单测 + 边界测试；
2. **分片构建工具**——10 分片并行导入（复用 import + docid 前缀）；
3. **10 亿基准验证**——分片构建 + sysbench 全套回归（对照 1 亿库基线）；
4. **raft RPC 接线**（阶段 C）——raft_meta 消息接 RPC 通道（真实节点间）+ 扩容编排联调；
5. **docid 上限监控**——u32 边界预警（运维 Metrics）。

## 8. 风险与缓解

| 风险 | 缓解 |
|---|---|
| docid 超 u32（20 亿+） | 分片前缀低 40 位预留 1 万亿/分片；超限时 Roaring64 改造（远期，独立设计） |
| 跨分片聚合查询延迟 | 广播检索 Chunk 拼接 O(1)（design 5.2.1）；10 分片基准验证 |
| 扩容中数据一致性 | scale_out 排空校验（未排空禁切）+ 回滚预案（7.76）已就绪 |
| 多副本 failover | raft 阶段一（7.77）已就绪；阶段 C RPC 接线后自动切换 |
| 构建/导入中断 | 批量导入模式 + WAL 崩溃恢复；分片独立构建互不影响 |

## 9. 验收标准（10 亿库）

- **构建**：10 分片并行 10 亿行完成（≤ 60 分钟），数据完整（抽样点查/COUNT 校验）；
- **点查**：10 分片 ≥ 10 万 QPS（8 线程/分片 × 4 分片以上，sysbench）；延迟 avg ≤ 1ms；
- **写入**：≥ 20 万 rows/s（分片并行 + 组提交）；
- **倒排**：亿级 posting 分页/COUNT sub-ms（K 项分块快速路径）；广播检索跨分片正确；
- **扩容**：新增分片在线加入（scale_out 编排）不中断服务、数据一致（排空校验）；
- **故障**：分片节点宕机 → raft 自动 failover（master 切换）≤ 心跳超时 + 选举；
- **回归**：既有 500 测试全绿；1 亿库对照无回归。

## 10. 触发与排期

- **阶段 A**（P0 级）：全局 docid 分配器——10 亿库扩展的前提，可独立先行（不依赖分布式联调）；
- **阶段 B/C/D**：随 10 亿构建与多节点部署推进（依赖硬件/节点规模）；
- **远期**：Roaring64（docid 20 亿+）、存算分离彻底化（10 亿级规模，design_remain 已评估代理层）。
