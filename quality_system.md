# 山水存迹数据库 · 质量证明体系（一证通）

> 版本：v1.0 · 适用：阶段 1（单机 MVP）及后续每个里程碑
>
> 一句话总结：**静态分析管「写法」，架构评审管「设计」，单元测试管「逻辑」，集成测试管「组合」，基准测试管「性能」，混沌测试管「生存」**——六层递进、缺一不可，共同构成山水存迹数据库「高质量」的完整证据链。

---

## 一、为什么需要一套可证明的质量体系

山水存迹数据库（shanshui-cunji）与山水观心操作系统同源——**Rust 编写、面向边缘场景**：在有限内存下保证 ACID，在弱网环境下保证数据一致性。

"高质量"不能只靠承诺，必须**可验证、可复现**。本体系沿用操作系统已验证的思路，从静态分析到混沌测试逐层递进，把质量证据固化为文档、报告与徽章，让读者（使用者、评审者、审计者）可以按图索骥、自行复核。

```
证据链方向（自底向上，成本递增、置信度递增）：
  静态分析 → 架构评审 → 单元测试 → 集成测试 → 基准测试 → 混沌测试
```

---

## 二、六层质量证明体系

### 第 1 层 · 静态分析（编译期即拦截问题）

| 工具 | 用途 | 触发时机 |
| --- | --- | --- |
| `cargo fmt` | 代码格式强制统一 | 每次提交 |
| `cargo clippy` | 代码风格 & 常见错误检查（`-D warnings` 零容忍） | 每次提交 |
| `cargo audit` | 依赖库安全漏洞扫描 | 每日 / 每周 |
| `cargo deny` | 许可证合规 + 重复依赖检测 | 每次 release |
| `cargo geiger` | 统计 unsafe 代码占比（目标 < 5%） | 里程碑节点 |
| `cargo tarpaulin` | 代码覆盖率统计（目标 ≥ 80%） | 每次 CI |

一条命令跑通全部静态检查：

```bash
cargo fmt --all -- --check \
  && cargo clippy -- -D warnings \
  && cargo audit \
  && cargo deny check \
  && cargo geiger
```

> 现状：`cargo fmt --check` 与 `cargo clippy -- -D warnings` 已在开发机常态化执行；
> `cargo audit`（99 依赖 0 漏洞）与 `cargo deny check`（许可证/重复依赖全 ok）已在本机执行通过；
> `geiger / tarpaulin` 由 CI / 里程碑节点执行（见 quality_system_process.md）。

### 第 2 层 · 架构评审（AI 大模型评估）

山水存迹数据库的核心价值是面向边缘场景的**架构取舍**。这些设计决策必须经得起审查。

- **评估方式**：每里程碑一次，让 AI（Claude / GPT / DeepSeek）对照设计文档审查代码。
- **评估维度**：
  1. 设计一致性——代码是否偏离原定架构（design.md / development.md）；
  2. 存储引擎分析——LSM / 稀疏索引 / 块缓存 / 热缓存的内存占用是否符合边缘预算；
  3. 事务与并发审查——锁机制、seq 单调性、WAL 崩溃恢复是否可能死锁 / 丢数据；
  4. 一致性协议——若实现分布式（阶段 2 Raft / Paxos），专项评估。
- **Prompt 模板**：

```text
你是数据库系统架构评审专家。请阅读山水存迹数据库的设计文档和核心代码，从以下维度评估：
1. 存储引擎设计是否匹配边缘设备内存约束。
2. 事务 ACID 实现是否正确（原子性 / 隔离性 / 持久性）。
3. 并发控制是否存在死锁风险。
4. 若使用 Raft/Paxos，协议实现是否完整。
输出：风险清单 + 改进建议。
```

- **产出**：`quality/architecture-review-{milestone}.md`（风险清单 + 整改闭环）。

### 第 3 层 · 代码审查（AI 辅助 + 自检清单）

| 方式 | 做法 |
| --- | --- |
| AI PR Review | 每次 PR 由 AI 做 Code Review，输出风险点 |
| 自检清单 | 提交前逐项核对（见下） |
| 每周回看 | 以"局外人"视角重审本周代码 |

**数据库代码自检清单**：

- [ ] 新增存储引擎代码是否影响内存预算？
- [ ] WAL / Redo Log 是否正确实现 crash-safe（重开不截断、seq 接续）？
- [ ] 锁的获取顺序是否与现有逻辑一致（防死锁）？
- [ ] 是否引入新的 `unsafe` 块？是否附带 safety 注释？
- [ ] 新增索引结构是否经过正确性验证（读回 / 重启 / 损坏注入）？

> 项目已落地：WAL 重开追加恢复、主键大端序、PAX/TTL/Delta 均配回归测试；
> 全量问题与修复见 [problem_solving.md](problem_solving.md)（P1~P36）。

### 第 4 层 · 单元测试 + 集成测试（覆盖率 ≥ 80%）

- **单元测试**证明"代码逻辑是对的"：LSM 插入/删除/查找、WAL 写入/回放/崩溃恢复、Tombstone、
  PAX 列式拆合、TTL 分桶过期、Delta 合并、序列化往返……
- **集成测试**证明"组合起来是对的"：Engine 全链路、HTTP-JSON 端到端、备份还原、跨重启持久化、
  倒排回表、多 CF（primary/cidx/delta）协同。
- **要求**：测试与代码同 PR（不允许后补）；覆盖率低于 80% 的 PR 不允许合并；新功能先写测试。
- **现状**：**313 个单元测试全绿**（`cargo test`），demo 冒烟 10/10（10 万~5000 万规模压测报告在 `images/`）。

### 第 5 层 · 基准测试（回答"好不好"）

| 指标 | 定义 | 工具 |
| --- | --- | --- |
| TPS / QPS | 每秒事务 / SQL 执行数 | Sysbench / demo 压测 |
| 延迟 P50/P90/P99 | 请求响应时间百分位 | latte / rust-db-bench |
| 并发能力 | 多线程下的性能表现 | Sysbench `--threads=N` |
| 内存占用 | 进程常驻内存、不持续增长 | 系统监控 |

**验收标准**：持续压测 60 秒 TPS 不显著下降；P99 < 100ms（边缘场景）；内存不泄漏。

> 现状：demo 冒烟内置 10 项功能基准（插入/点查/缓存/组合索引/倒排/分片/删除/备份），
> **v0.5.0**（2026-08-29，release + 默认 mimalloc，i7-10750H 6C12T / 16G / NVMe）：YCSB 纯读冷缓存 **18 万 ops/s**（P50≈5µs）、
> 热缓存 87 万 ops/s、100 万 SST 分层读不掉速；demo 1000万 插入 29.3 万条/s（10/10）；实证 fsync 单条串行为写瓶颈（55×）→ Group Commit 列入下里程碑；
> **v0.4.0**（2026-08-28）：1000万条插入 **38.6 万条/s**（25.9s，10/10）、
> 倒排词条 2.1s（250 万命中 + 20 万回表全对）、备份·还原 20.7s；M6 功能改动（环形 WAL / Leveled-Compaction /
> MVCC 快照读 / 热点保护区 / 增量备份）无性能回归（环形 WAL 默认关闭、Leveled 仅 compact 路径）；
> 历史基线：v0.3.0 三规模 29.5~33.8 万条/s（见 `images/perf-0.3.0/`）、分配器专项见 `images/allocator-bench/`（musl+mimalloc 4~10 倍）；
> 实测中发现并修复读路径回归（`get_from_sst` 克隆整个 Level 2 索引，见 problem_solving P30）。

### 第 6 层 · 混沌测试（证明"在故障中不出事"）

| 故障类型 | 模拟方式 | 验证内容 |
| --- | --- | --- |
| 进程崩溃 | kill -9 | WAL 回放后数据完整 |
| 主从切换 | 模拟主节点故障 | 恢复时间、数据一致性（阶段 2） |
| 磁盘写满 | 填充至 100% | 错误处理不崩溃 |
| 网络分区 | iptables 阻断 | 一致性协议正确处理（阶段 2） |
| 高并发写冲突 | 大量并发写同一行 | 不产生死锁 |

**验收标准**：任一项混沌测试后数据库可恢复；已提交事务不丢失；checksum 校验通过。

> 现状（MVP 已完成部分）：WAL 尾部截断恢复、SST 损坏注入拒载、孤儿倒排段忽略、
> Engine 跨重启数据 + 倒排保留（步骤 16 测试，见 [images/linux-musl](images/linux-musl/验证记录.md)）；
> **磁盘写满**：P31 实测中 C 盘 100% 满真实触发——进程错误退出而非崩溃、无数据损坏，修复环境后恢复（见 problem_solving P31）；
> **环形 WAL 崩溃恢复（M6-1）**：两阶段 fsync（记录区 → 头部 tail）+ 回绕覆盖安全（全刷盘前提 + WalFull 强制 Flush），
> 测试验证崩溃重开只回放已 sync 记录（见 problem_solving P35）。

---

## 三、对外交付物

1. **CI 徽章**（README 头部）：Build passing / Coverage ≥80% / Unsafe <5%
2. **质量报告**（每里程碑，存放 `quality/`）：
   - 内存基线报告（各模块占用明细）
   - 性能基准报告（TPS/QPS/延迟 + 对比基线）
   - 测试覆盖报告（tarpaulin HTML）
3. **安全证明**：unsafe 清单 + 每条 safety 注释审查状态；已知问题清单公开透明（problem_solving.md）
4. **可复现性**：一键命令（见 quality_system_process.md）

---

## 四、全景图

```text
┌─────────────────────────────────────────────────────────────┐
│                     持续集成（CI）                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌──────┐ │
│  │clippy   │ │rustfmt  │ │ audit   │ │tarpaulin│ │ deny │ │ ← 每次提交
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └──────┘ │
├─────────────────────────────────────────────────────────────┤
│                   架构评审（AI 大模型）                      │
│  · 设计一致性检查（设计文档 vs 代码）   · 每里程碑一次      │
│  · 存储引擎/事务/并发专项审查                              │
├─────────────────────────────────────────────────────────────┤
│                   代码审查（AI 辅助 + 自检清单）             │
│  · 每次 PR 自动 AI Review                  · 每次 PR       │
│  · 自检清单（数据库专用）                   · 每周回看      │
├─────────────────────────────────────────────────────────────┤
│                   单元测试 + 集成测试（覆盖率 ≥80%）          │
│  · 单元测试（LSM/WAL/PAX/TTL/Delta）       · 每次 commit   │
│  · 集成测试（Engine 全链路/HTTP/崩溃恢复）  · 每次 PR       │
├─────────────────────────────────────────────────────────────┤
│                   基准测试 + 混沌测试                        │
│  · demo 功能基准 + Sysbench OLTP（TPS/QPS/延迟）· 每里程碑  │
│  · 崩溃/断电/磁盘满/网络分区故障注入                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、与项目现状的对应关系

| 体系层 | 项目落地 |
| --- | --- |
| 静态分析 | `cargo fmt --check` + `cargo clippy -D warnings`（开发机常态化）；`cargo geiger` 正式报告：**项目自身 unsafe = 0**，源码已加 `#![forbid(unsafe_code)]` 编译期强制 |
| 架构评审 | design.md / development.md 双文档 + 每里程碑 AI 评审（quality/） |
| 代码审查 | problem_solving.md（P1~P36 问题闭环）+ 提交前自检清单 |
| 单元/集成测试 | **313 测试全绿** + demo 冒烟 10/10 + HTTP 端到端 |
| 基准测试 | demo 10 项功能基准（10万~5000万压测）+ 组提交 91,296 ops/s（v0.6.0 快检）|
| 混沌测试 | WAL 截断恢复 / SST 损坏注入 / 孤儿段忽略 / 跨重启持久化 |
