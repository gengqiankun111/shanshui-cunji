# 山水存迹数据库（shanshui-cunji）设计文档

> 版本：v0.1（MVP 单机版设计稿 + 分布式演进蓝图）
> 状态：设计稿，开发路线遵循「先单机，后分片」

---

## 1. 产品定位

**一句话定位：**

> 一款面向海量结构化文档，主打高吞吐写入、热点主键极速查询、任意字段灵活筛选；可水平扩展至 **50 亿级**数据；弱化跨分片事务，适合日志、埋点、标签画像、元数据存储的高性能分布式文档-检索数据库。

### 1.1 核心能力

1. **热点主键查询极快**：内存哈希缓存命中，亚毫秒返回；
2. **固定条件查询对标 MySQL**：走组合稀疏索引，无需全表扫描；
3. **任意字段自由 AND/OR 筛选**：原生倒排索引（内存字典 O(1) 定位 + 追加式倒排文件），不必预先建一堆组合索引；
4. **容量大**：单节点设计基准 **1.5 亿文档（约 300GB 磁盘）**，分片集群线性扩展至 **50 亿级**；
5. **单点读写延迟远优于 TiDB 类强一致分布式数据库**；
6. **明确短板**：不带分片键的全局检索是广播慢查询，不适合超高并发核心路径。

### 1.2 设计哲学：放弃什么，换取什么

| 放弃 | 换取 |
| --- | --- |
| 跨分片事务 / 全局分布式锁 | 高吞吐写入、低延迟查询 |
| 跨分片 JOIN | 水平扩容简单、单点性能保留 |
| Raft 强同步（默认） | 单点写入延迟 ≈ 单机性能 |
| **机械硬盘（HDD）兼容** | **SSD 原生存储格式红利**（见 4.8） |

**原则：先单机，后分片；先存得进、查得出，再优化上亿条性能。**

**存储介质定位（v0.7 起正式确认）**：shanshui-cunji **只支持 NVMe / SATA SSD**，不再考虑机械硬盘（HDD）兼容。放弃 HDD 兼容换来：接受随机 I/O（消除合并开销）、块大小对齐 SSD 页（4KB）、WAL 环形单文件、允许空间放大换取写入放大降低、并行 Compaction 等原生优化——核心路径代码量减少 30%~50%，性能提升 30%~60%（详见 4.8）。

> ⚠️ **开发环境警告**：本数据库的存储格式按 SSD 优化设计（4KB 块 / 环形 WAL / 删除位图等）。**开发环境若使用机械硬盘，写入与查询性能会大幅下降（可达 10 倍以上差距），压测数据不具备参考价值——不要压测**。所有性能验证必须在 SSD 上进行。

### 1.3 生态定位：新旧系统融合策略

**存储介质确认**：shanshui-cunji 是**纯 SSD 持久化（Disk-Based，NVMe/SATA SSD Only）**——数据写入必须经 **WAL fsync + 落盘 SSTable** 才算真正成功；HotCache / BlockCache / 倒排字典仅是**内存加速层（Cache）**，断电或重启后缓存清空、磁盘数据完好，WAL 恢复即可。数据量仅受磁盘限制（单机 1.5 亿基准 / TB 级），而非像 Redis 受内存限制。**不支持机械硬盘（HDD），开发/测试环境使用 HDD 时性能大幅下降、勿压测（见 1.2 警告与 4.8）。**

shanshui-cunji 不是要"干掉"MySQL 或 Redis，而是作为它们的**容量与速度扩展层**：

1. **与 MySQL（交易库）的关系**：
   - MySQL 管事务（增删改、强一致）；shanshui-cunji 管海量（埋点、日志、归档）；
   - 数据同步：通过 **Canal / Debezium** 监听 MySQL binlog，实时同步到 shanshui-cunji 作为**只读分析副本**，规避跨库 JOIN 对主库的压力；
2. **与 Redis（缓存库）的关系**：
   - Redis 管超高并发热数据（微秒级）；shanshui-cunji 管全量持久化（毫秒级 + 倒排检索）；
   - 推荐范式：**Cache-Aside（读回填）+ Write-Invalidate（先落盘后失效）**，SDK 内置 `shanshui-cunjiWithRedis` 门面（详见 [第 21 章](#21-外部缓存集成redis-适配器)）；
   - 冷热分层收益：用 1 份 Redis 内存成本（只存 1 小时热数据）换无限容量的全量历史查询；shanshui-cunji 仅作冷存储时 CPU 几乎全留给写入任务；
3. **替代遗留系统（MongoDB / ES）的明确条件**：
   - 若 MongoDB / ES 仅做**结构化文档存储与简单筛选**（无嵌套查询、无聚合管道、无全文检索），切换至 shanshui-cunji 可降低 ~60% 硬件成本、提升 3~5 倍写入速度（ES 结构化场景可 10 倍）；
   - **绝对禁止替代**：银行交易、库存扣减、购物车结算、账户余额（需跨行事务，保留 MySQL/PostgreSQL）。

**一句话总结**：*MySQL 管钱，Redis 管热，shanshui-cunji 管海量——三者搭配，各司其职。*

### 1.4 竞争定位：在特定领域做到极致

**优势（为什么选它）：**

- **写入吞吐性价比**：LSM-Tree 提供比 MongoDB 更高的写入性能；放弃跨分片事务，避免了 NewSQL（TiDB/CockroachDB）为强一致性付出的性能开销；
- **查询灵活度**：组合索引 + 倒排索引比 Cassandra/HBase 宽表更灵活；无 JOIN 场景下查询延迟远低于 Spark/Flink 类平台；
- **资源成本 / 运维门槛**：单机内核远轻于 NewSQL（建议 32GB+ 内存）；部署、运维、硬件成本更低；
- **独有杀手锏**：聚合（倒排统计载荷毫秒级 COUNT/GROUP BY，7.2）+ TTL 时间分区（过期整删文件无碎片，5.4）。

**劣势与边界（清醒认知）：**

- ❌ **无跨分片事务**：金融交易、库存扣减等强一致场景禁用；
- ❌ **分析能力弱**：无 JOIN / 子查询，适合当"数据源"而非"分析引擎"；
- ⚠️ **无分片键全局检索是广播慢查询**：随集群规模增大而劣化；
- ⚠️ **功能丰富度不及通用库**：无嵌套文档、地理空间查询、完整生态工具链。

**选型一句话**：需要强 ACID → TiDB/OceanBase；复杂 SQL/BI → ClickHouse/Doris；高度嵌套 JSON + 多变查询 → MongoDB；跨地域强一致多活 → CockroachDB；**日志 / 埋点 / IoT / 画像的海量写入 + 快速检索 → shanshui-cunji**。完整全景对比见 Readme 第 九 章。

---

## 2. 总体架构

### 2.1 单机版（MVP，第 1 阶段）

```
┌──────────────────────────────────────────────┐
│                 网络服务层                     │
│   TCP 简易服务 / HTTP-JSON 查询接口 / CLI     │
└─────────────────────┬────────────────────────┘
                      │
┌─────────────────────▼────────────────────────┐
│              单机存储引擎内核                  │
│  ┌─────────────┐   ┌──────────────────────┐  │
│  │  HotCache   │   │   查询执行器          │  │
│  │ 热点哈希缓存 │   │ 主键/组合索引/倒排    │  │
│  └──────┬──────┘   └──────────┬───────────┘  │
│         │                     │              │
│  ┌──────▼─────────────────────▼───────────┐  │
│  │            LSM-Tree 存储引擎            │  │
│  │  WAL → MemTable(跳表) → SSTable(多层)   │  │
│  │  主键索引 / 组合稀疏索引 / 原生倒排    │  │
│  │  布隆过滤器 / 块缓存 / Compaction        │  │
│  └─────────────────────────────────────────┘  │
└───────────────────────────────────────────────┘
```

### 2.2 分布式版（第 2 阶段蓝图）

```
┌─────────────────────────────────────────────┐
│          网关路由层 Proxy-Gateway（无状态）   │
│  分片路由 · 请求分发 · 结果合并(DocId)        │
│  读写路由 · 限流 · 监控 · 故障转发            │
└───────────────┬─────────────────────────────┘
                │
┌───────────────┼──────────────┐
│               │              │
┌─────▼────┐ ┌──▼─────┐  ┌─────▼────┐
│ 分片-0   │ │分片-1   │ …│ 分片-N   │
│ (单机内核)│ │(单机内核)│  │(单机内核)│
└─────┬────┘ └──┬─────┘  └─────┬────┘
      │         │               │
┌─────▼─────────▼───────────────▼─────┐
│  元数据中心 Meta-Server（轻量，主备） │
│  分片路由表 · 健康状态 · 迁移任务      │
└───────────────────────────────────────┘
```

架构原则：**计算下沉分片，网关只做转发与简单结果合并，不做重型跨分片计算。**

---

## 3. 数据模型与 API

### 3.1 文档模型

- 每条记录 = 一个文档，主键为 `DocId (u64)`；
- 文档由多个字段组成：`DocId -> {field1: value1, field2: value2, ...}`；
- 字段为扁平 key-value（结构化字段），支持字符串、数值、布尔、时间等标量类型；
- 底层以 LSM 有序键值对存储，不同数据走不同键空间：
  - 主数据：键为 `DocId`，值为完整文档（二进制序列化）；
  - 组合索引：键为 `Encode(field1, field2, ..., DocId)`，值为空或 DocId 列表；
  - 倒排索引：独立于 LSM 的「内存字典 + 追加式倒排文件」（见 5.2），不再以 LSM KV 存储。

### 3.2 基础操作（CRUD）

| 操作 | 说明 |
| --- | --- |
| `put(docid, doc)` | 写入/覆盖一条文档 |
| `patch(docid, fields)` | 部分字段更新（阶段 1.5，见 4.7） |
| `get(docid)` | 主键读取 |
| `delete(docid)` | 删除（墓碑标记） |
| `range(start, end)` | 主键范围扫描 |
| `search(filter)` | 条件检索（组合索引 / 倒排） |

### 3.3 网络接口

- MVP 提供 **HTTP-JSON** 接口，方便上层 Web 后台直接调用；
- 同时提供 **TCP 简易协议**（紧凑二进制，减少边缘环境开销）；
- CLI 客户端：`shanshui-cunji` 命令行工具，支持增删查改、备份还原、状态查看；
- 查询协议支持**类 SQL WHERE 子句**（基于 `sqlparser-rs`，仅 `SELECT ... WHERE AND/OR` 子集），降低从 MySQL 迁移的学习成本；**不承诺 MySQL 方言兼容**，不支持 JOIN / GROUP BY / 子查询 / 事务，详见第 15 章迁移与兼容策略。

### 3.4 数据模型细节

**键编码规范：**

| 索引 | 键格式 | 编码 |
| --- | --- | --- |
| 主键 | `DocId` | u64 定长 |
| 组合稀疏索引 | `Encode(field1, field2, ..., DocId)` | 变长整数 + 分隔符 |
| 倒排索引 | `TermData := Term ++ RoaringBitmap(DocId 列表)` | 追加式倒排文件（见 5.2），不走 LSM 键 |

- **变长整数（Varint）+ 分隔符**编码（仅适用于 LSM 键空间：主键 / 组合索引）：DocId 等定长字段按固定宽度存储，字符串等变长字段按长度前缀 + 原始字节存储；
- 编码保证键有序性：先按字段值排序，同字段值内按 DocId 排序，从而支持前缀扫描与范围查询。

**文档序列化格式：**

- MVP 采用**自定义二进制序列化**：字段名用字典编码压缩为 u16 字段 ID，值为带类型标签的标量（`TypeTag + Varint / Length-Prefixed Bytes`），避免重复字段名的空间浪费；
- **字段注册表持久化（FieldRegistry，必做）**：字段名 ↔ u16 ID 映射必须**持久化为元数据文件**（`metadata/fields.idx`），而非仅存内存；启动时先读元数据，若文档中出现未注册字段则**自动扩展映射并持久化**，防止重启后旧数据因字段 ID 映射丢失而反序列化失败，天然支持 Schema 演进；
- HTTP-JSON 接口层负责 JSON ↔ 内部二进制转换，外部交换走 JSON，内部存储统一二进制；
- 二期可选：迁移 FlatBuffers / Cap'n Proto，支持零拷贝访问子字段，避免反序列化整个文档。

**列族划分：**

- 主数据、组合索引、倒排索引分别归属**独立列族（Column Family）**，各自拥有独立的 MemTable / SSTable / 缓存 / Compaction：

| 列族 | 内容 |
| --- | --- |
| `cf_data` | 主数据（DocId → 完整文档） |
| `cf_cidx` | 组合稀疏索引 |
| `cf_inv` | 倒排索引 |
| `cf_delta` | 增量更新（阶段 1.5，见 4.7） |

- 收益：compaction 互不干扰、缓存按类型独立调优、可按列族分别生成布隆过滤器；
- **物理目录隔离**：每个列族独立目录（`data/cf_data/`、`data/cf_cidx/`、`data/cf_inv/`、`data/cf_delta/`），TTL 时间桶子目录按列族独立、可分别配置——避免混排导致 TTL 无法按列族过期：

```
data/
├── cf_data/    # 主数据（时间桶：2026-08-26/ ...）
├── cf_cidx/    # 组合稀疏索引
├── cf_inv/     # 倒排索引
└── cf_delta/   # 增量更新（阶段 1.5）
```

---

## 4. 存储引擎（单机内核，核心中的核心）

技术路线：**LSM-Tree**（最适配日志、时序、事件类海量写入场景）。

### 4.1 写入路径

```
写请求 → WAL(预写日志) → MemTable(内存跳表) → 后台刷盘 → SSTable(有序文件) → Compaction
```

- 高性能模式（可选，阶段 3，默认关闭）：写入经 **io_uring（SQPOLL 模式）异步提交**，WAL 使用**预分配环形文件**，刷盘走**直接 I/O（O_DIRECT）**绕过页缓存，减少内核态切换与内存双重占用；默认保留传统同步路径保证兼容性（详见 4.3）；
- **写 Stall 机制（必须可配置）**：当 L0 文件数超过阈值 `l0_stall_threshold`（默认 8）时，通过 tokio Semaphore 限流主动降低前台写入速率，防止 Compaction 跟不上导致内存爆炸；阈值可调，与 Compaction 速率联动。

### 4.2 MemTable

- **使用跳表 Skip-List**（有序、范围查询友好、并发插入易实现，RocksDB 默认方案）；
- **双 MemTable 切换**：一块接收新写入，另一块冻结准备刷盘，**刷盘不阻塞写入**，消除写入抖动。

### 4.3 WAL 预写日志

1. **批量组提交（Group Commit）**：攒一批记录一次性刷盘，杜绝每条记录一次 `fsync`；
2. **循环分段日志文件**：定期切分，避免无限追加；**旧文件延迟删除（Deferred Deletion）**——不直接 `unlink`（GB 级大文件 unlink 是同步阻塞的元数据操作，持目录 inode 锁达 ~100ms，导致新写入瞬时 P99 毛刺），而是先重命名为 `to_delete_xxx`，交给低优先级后台线程在业务空闲期异步 unlink（或 `fallocate` 打洞后再删）；
3. **双写入模式**（可配置）：
   - 强安全模式：每次提交刷盘（默认）；
   - 高性能模式：延迟刷盘（边缘场景客户愿牺牲极小安全性换取吞吐）。
4. **高性能 WAL 模式（可选，阶段 3，默认关闭）**：
   - **io_uring 异步 IO（SQPOLL）**：提交异步写入队列，应用层无阻塞，降低 CPU sys 态占用（Rust 侧经 io-uring crate 接入）；
   - **环形 WAL（Ring File）**：预分配固定大小文件，写入指针循环移动，省去文件扩展与元数据更新开销；
   - **直接 I/O（O_DIRECT）**：刷盘绕过 OS 页缓存，减少内存双重占用，避免 Page Cache 抖动影响 HotCache；
   - 收益：NVMe 下写入延迟降低 30%~50%，吞吐逼近磁盘物理极限；代价是依赖 Linux 内核生态。
5. **现状与 SSD 原生方向（v0.6 已落地）**：环形 WAL 已成为可选模式之一（`[storage] wal_mode="ring"`，M8-P12 头部 tail 合并 fsync，sync 单次原子提交）；SSD 原生下进一步向**大文件环形 + 环形指针**演进（消除文件切换开销、磨损均衡），见 4.8 阶段二。

### 4.4 SSTable 磁盘文件

文件布局：

```
┌─────────────────────────────┐
│ File Header (magic, 版本)    │
├─────────────────────────────┤
│ Data Block 0 (PAX 混合列组)  │
│ Data Block 1                │
│ ...                         │
├─────────────────────────────┤
│ Block Index (稀疏索引 + Zone Map) │
│ Bloom Filter (布隆过滤器)     │
├─────────────────────────────┤
│ Footer (各段偏移/长度/统计)    │
└─────────────────────────────┘
```

关键点：
1. **块内布局 = PAX 混合列式存储**：数据块不再按"行"顺序存放整篇文档，而是**按列分组（Column Group）**：
   - **热列组（块头部）**：高频查询字段（`DocId`、`status`、`type` 等）连续存放，块头附**列偏移量表**（每组列的偏移/长度）；
   - **冷列组（块尾部）**：低频大字段（`payload`、`extras` 等）靠后存放；
   - 点查 / 条件筛选只读取块头部的热列切片，**跳过 90% 无用大字段 IO**；宽表场景延迟从"读整块"变为"读列切片"，P95 显著下降，单机 QPS 提升 2~3 倍；
   - 热字段白名单**用户可配置**（SST 生成时按配置划分列组）；
2. **列组内前缀压缩**：热列组内按键采用**增量编码**（`abcd` 与 `abef` →「共享前缀长度=2 + 后缀 `ef`」）；值按 4.4.2 做**块级压缩**（默认 Zstd Level 3）；
3. **稀疏块索引 + Zone Map**：块索引是**磁盘结构**（SST 文件内的 Block Index 段），每条目记录「块首键 + 块偏移 + 块长度 + **Zone Map 元数据**」；**内存只保留每个 SST 的稀疏副本**（块首键 + 偏移 + Zone Map），定位数据块时：内存稀疏索引二分 → **Zone Map 粗筛**（见 4.4.1）→ 精确跳转到磁盘块 → 块内顺序扫描；
4. **布隆过滤器（MVP 必做）**：查询不存在的 key 直接跳过整个 SST，避免无效磁盘读取，上亿数据集下收益数倍；**配置**：按各列族独立生成（主键 / 组合索引 / 倒排 term 各一份），每 SST 按预估 key 数分配位数组长度（默认 key 数 × 10 bits，假阳性率 ≈ 1%），哈希函数 7 个（双哈希派生）；
5. **块缓存（LRU）**：高频访问的数据块驻留内存，命中即免磁盘 IO。
6. **块大小（SSD 原生，v0.7 起）**：默认 `block_size_kb` 从 16KB（对齐 HDD 扇区/寻道）调整为 **4KB（对齐 SSD 页）**——点查读放大从 16× 降到 4×；索引更密、定位更准（配合两级索引控制内存，见 4.4 关键点 3 的两级稀疏索引）；压缩率略降（-5%），换取读放大 -75%、点查延迟 -60%。仍支持配置回退大块（如 `block_size_kb=64` 的 NVMe 顺扫场景）。

### 4.4.1 块级 Zone Map 索引

每个数据块在 Block Index 中附带**轻量级统计元数据（Zone Map）**：

- 该块内各字段的 **min / max / null 计数**；字符串字段采样前缀最小/最大值；
- **与布隆过滤器互补**：布隆解决"等值是否存在"，Zone Map 解决"数值 / 时间范围剪枝"；
- **查询剪枝逻辑**：扫描 SST 时，先对目标块做 Zone Map 判断——范围条件（如 `age > 30`）与块内 min/max 无交集即跳过该块，**无需读块、更无需解压**；
- 收益：`age > 30 AND status = 'active'` 这类组合条件可直接跳过 70%~90% 的数据块；
- **与倒排互补**：倒排解决"term 命中"，Zone Map 解决"范围剪枝"；倒排拿到 DocId 回表读文档前，同样先过 Zone Map。

### 4.4.2 存储编码与压缩策略

**键 / 值编码：**

- **Varint 编码（LEB128）**：DocId 与长度字段改用变长整数——小整数 1~2 字节、大整数渐进增长；百万级 DocId 从 8 字节压缩到 3~4 字节，数据块整体缩小 30%~50%；
- **差值编码（Delta Encoding）**：SSTable 内键有序，只存与前一个键的差值（差值再 Varint）——索引块大小减少 50%~70%；
- **倒排 DocId 列表**：Roaring Bitmap（Array / Bitmap / Run 三容器自适应，见 5.2）。

**数据块压缩：**

- 每个 Data Block **独立压缩**，算法可选：`none` / `snappy` / `lz4` / `zstd`；
- 默认 **Zstd Level 3**：平衡压缩率（~50%，总存储降至原始 20%~30%）与速度；归档 / 冷数据可调 Level 8+；
- 压缩在 Block 写入时完成，读取时解压后进入 BlockCache；
- 每 Block 独立 **CRC32**（Block Trailer）：损坏只影响单个 Block；Footer 含各段偏移与校验和，启动快速校验。

**索引块两级结构（阶段 2）：**

- Level 1（内存常驻）：每 16 个 Block 一条摘要（`sstable.index_granularity`），极轻量；
- Level 2（按需加载）：精确 Block 索引；
- 对比（1.5 亿文档）：单层稀疏索引 ~200MB → 两级索引 ~20MB，内存减少 90%，更多留给 HotCache。

**布隆过滤器优化：**

- 分区布隆（Partitioned Bloom，阶段 1.5）：按数据块分区生成，查询只加载目标块布隆，内存减少 ~50%；
- Ribbon Filter（阶段 3，可选）：RocksDB 方案，更快更省内存；
- 假阳性率可配：`sstable.bloom_fpr = 0.01`（默认）。

**键值分离（KV Separation，阶段 3 可选）：**

- 参考 WiscKey / Badger：SSTable 只存 Key + Value Log 指针（~16B），大 Value 存 Append-Only Value Log；
- 收益：Compaction 只重排 Key、不拷贝大 Value，写放大降低 50%~80%；
- 代价：点查多一次随机读（Value Log），需权衡。

**配置项：**

| 配置 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `sstable.compression` | enum | "zstd" | none / snappy / lz4 / zstd |
| `sstable.compression_level` | u32 | 3 | zstd 专用，1~22 |
| `sstable.bloom_fpr` | f64 | 0.01 | 布隆假阳性率 |
| `sstable.index_granularity` | u32 | 16 | 两级索引：每 N 个 Block 一条摘要 |

> 上述优化与 PAX 列组、Zone Map 设计共存，属增量增强，无需推翻现有架构。

### 4.5 Compaction

- MVP：先用最简单的合并策略跑通功能链路；
- 二期：采用 **Leveled-Compaction 分层压实**（RocksDB 方案），限制单次压实数据量，避免大合并瞬间打满 IO 导致写入卡顿；
- 后台压实线程与用户读写线程做 **IO 优先级隔离**，防止压实抢占查询带宽；Compaction 线程池使用**最低 I/O 优先级（ionice）**或 Rust 异步调度器权重，并限制每秒读写字节数（可配置 `compaction.rate_limit_mb/s`）；
- **倒排文件 GC（见 5.2.2）**：追加式倒排文件超过阈值后，后台重写为紧凑文件、原子替换内存指针，不阻塞前台查询。
- **SSD 原生参数（v0.7 起）**：允许**空间放大换取写入放大降低**——L0→L1 触发阈值放宽（4 文件 → 8~12）、层级比例 10× → **20×**、Compaction 并行度可调（1 → 2~4 线程，SSD 并发 IO 强）；目标写入放大（WA）从 15~25× 降至 6~10×（-60%），空间放大 1.2× → 1.8×（SSD 空间便宜，可接受）。

### 4.6 删除与更新

- **Tombstone 墓碑标记（MVP 必做）**：删除不立刻抹盘，写入一条墓碑记录，后台 compaction 再清理旧数据；
- 更新 = 写入带新版本号的记录，旧版本由 compaction 回收；
- 二期可选：MVCC 多版本快照读，读写互不阻塞（MVP 暂不做，控制开发量）。
- **SSD 原生：删除位图（Deletion Bitmap，v0.7 阶段二）**：独立于 LSM 维护按 DocId 的 1bit 删除位图（4KB 页对齐 + mmap）——删除仅写 1bit + fsync 1 页（-99% IO），查询 O(1) 跳过已删文档，墓碑不再污染 LSM 层级；compaction 物理删除后清除位图标记（见 4.8 阶段二）。

### 4.7 增量更新列族（Partial Update / Patch，阶段 1.5）

**痛点**：设备上报只改 `temperature` 一个字段，却要 PUT 全量文档（如 1KB），浪费 10 倍网络 IO 与 LSM 写入带宽。

**方案：Base CF（全量）+ Delta CF（增量）双层，Merge-on-Read。**

- **Base CF（主数据列族）**：存全量文档；
- **Delta CF（增量列族）**：键为 `DocId + FieldName`，值为新值（带版本号 / 时间戳）；写入 Patch 只追加一条几十字节的小记录；
- **读取 Get**：先读 Base 全量 → 再读 Delta 全部增量 → 按时间戳应用覆盖 → 返回合并后的最终文档；
- **Compaction 净化**：后台合并时，足够老的 Delta 直接压实进 Base，消除碎片；
- **收益**：写入 IO 放大从 10 倍降为接近 1 倍，尤其适合 IoT 宽表；
- **API**：`patch(docid, {field: value, ...})` 替代全量 PUT。

### 4.8 SSD 原生存储优化（v0.7 起：放弃 HDD 兼容）

> **定位**：本数据库自 v0.7 起**仅支持 NVMe / SATA SSD**，不再兼容机械硬盘（HDD）。
> 本节汇总 SSD 原生存储格式改进 + 「写入快 + 多倒排字段」场景优化优先级。
> ⚠️ **开发环境若使用机械硬盘，性能大幅下降（10 倍级差距），压测无参考价值——不要压测**；
> 所有性能验证必须在 SSD 上进行（见 1.2 警告）。

#### 4.8.1 核心设计转变（HDD 兼容 → SSD 原生）

| 优化维度 | HDD 友好设计（旧） | SSD 原生设计（新） | 收益 |
| --- | --- | --- | --- |
| 随机 I/O 代价 | 尽量合并为顺序 I/O | 接受随机 I/O，消除合并开销 | 写入延迟 -30%，CPU -40% |
| WAL 布局 | 分段环形文件（每 1GB 切分） | 单一大文件 + 环形指针（预分配） | 省文件切换/rename/fsync |
| SST 索引粒度 | 粗粒度（减少随机读） | 细粒度（4KB 对齐 SSD 页） | 读放大降低 70%+ |
| 数据块大小 | 16KB~64KB（对齐 HDD 扇区/寻道） | 4KB（对齐 SSD 页） | 读放大 -80% |
| LSM Compaction | 避免空间放大 | 允许空间放大换取速度 | 写入放大 -60% |
| fsync 频率 | 尽量少 fsync | 充分利用 SSD 缓存（可容忍小延迟） | 延迟更可预测 |
| 擦写均衡 | N/A | WAL 磨损均衡 + 冷数据迁移 | SSD 寿命 +300% |

#### 4.8.2 场景瓶颈：写入快 + 20 倒排字段

**单条写入 0.9ms 耗时分解（估算）**：

```
单条写入总耗时 0.9ms（目标）
├── 网络解析 + 路由：0.1ms
├── WAL 写入 + fsync：0.25ms   ← 瓶颈 1
├── MemTable 写入：0.05ms
├── 主数据 SST 写入：0.05ms
├── 20 个倒排字段更新：0.40ms  ← 瓶颈 2（最大）
│   ├── 每字段 Hash 查找：0.005ms × 20 = 0.1ms
│   ├── 每字段追加写入：0.010ms × 20 = 0.2ms
│   └── 每字段元数据更新：0.005ms × 20 = 0.1ms
└── 响应返回：0.05ms
```

→ 优化空间最大：**倒排更新 0.40ms → 目标 0.15ms**、**WAL 0.25ms → 目标 0.15ms**。
低基数字段（status 等）列表极长 → 高并发下倒排列表追加竞争；追加式倒排文件大量小 append →
元数据更新频繁；SSD Only 决策可接受随机写，释放 WAL 与倒排更新的设计约束。

#### 4.8.3 优化方向与优先级（P0 → P1 → P2）

**P0（必须做，1~2 个月，收益最直接）——目标写入 TPS 22 万 → 32 万（+45%）**

| # | 优化项 | 内容 | 写入快收益 | 投入/风险 |
| --- | --- | --- | --- | --- |
| 1 | 环形大文件 WAL | 预分配单文件 + 环形指针循环覆盖，省去 rename/open/切文件（v0.6 已有 RingWal 基础，扩展规模化 + 磨损均衡） | WAL P99 -60%（2ms→0.8ms） | ~800 行 / 中（崩溃恢复需重测） |
| 2 | 倒排更新批处理 | 每个写入线程攒批：同 Term 多 DocId 内存聚合，批量追加倒排文件；与 Group Commit 联动 | 倒排更新 CPU -60%（0.4→0.16ms） | ~500 行 / 低 |
| 3 | SSTable 4KB 块 + 两级索引 | Data Block 16KB→4KB，Block Index 两级（内存摘要 + 磁盘精索） | 回表读放大 -75%（16×→4×） | ~350 行 / 低（只改读路径） |
| 4 | 倒排分片锁 | 按 Term Hash 分 256 锁分区：同 Term 串行、不同 Term 并行 | 高并发锁竞争 P99 -40% | ~150 行 / 低 |

**P1（重要，3~4 个月，中长收益）——P0+P1 后写入 TPS 32 万 → 38 万（+73%）**

| # | 优化项 | 内容 | 写入快收益 | 投入/风险 |
| --- | --- | --- | --- | --- |
| 5 | 倒排 FST + Mmap 字典（5.2.4.1） | 20 字段 Term 基数大（1.18 亿）→ 内存字典 ~8GB；分层 FST + mmap，查询 O(len(term))，写入增量 delta.fst | 冷启动 8s→50ms，写入查表 -30% | ~600 行 / 中（需兼容现有字典） |
| 6 | 元数据-数据解耦 | SST 的 Block Index/Bloom/Zone Map 独立元数据区，Compaction 只重写元数据（倒排 TermMeta 同理） | 写放大 -50%，Compaction 干扰降低 | ~800 行 / 高（格式大改） |

**P2（进阶，2~3 个月）**

| # | 优化项 | 内容 | 收益 | 投入/风险 |
| --- | --- | --- | --- | --- |
| 7 | 冷热感知 Compaction + Bloom Merge | 按 SST 热度排序，合并前布隆判断有效性，优先合并热层级（倒排 GC 同样适用） | Compaction 写入量 -30% | ~500 行 / 中 |
| 8 | 多 SSD 条带化 | WAL 独占最快 SSD、SSTable 分布多盘、倒排独立放置 | 多盘部署写入 +3~4× | ~400 行 / 低 |

**最终目标**：写入 TPS 40 万+、写 P95 0.45ms、倒排更新 0.1ms、写放大 5×、冷启动 5s、锁竞争消除。

#### 4.8.4 配置模板（storage）

```toml
[storage]
mode = "ssd_optimized"        # v0.7 起仅支持 ssd_optimized（无 hdd_compatible）
block_size_kb = 4             # 数据块大小（对齐 SSD 页）
wal_mode = "ring"             # 环形 WAL
wal_ring_size_mb = 4096       # 预分配大小（按容量规划）
compaction_level_ratio = 20   # 层级比例（空间放大换写入放大）
compaction_threads = 2        # 并行 Compaction（SSD 并发 IO）
deletion_bitmap_enabled = true # 删除位图（阶段二）
inverted.shard_locks = 256    # 倒排分片锁分区数（P0）
```

#### 4.8.5 实施路线图

- **阶段一（P0，低风险快速收益）**：SST 4KB 块 + 倒排分片锁 + 倒排更新批处理 + Compaction 参数调优（20×/并行）；
- **阶段二（P1 核心改造）**：环形大文件 WAL 规模化 + 删除位图 + 倒排 FST/mmap 字典 + 元数据-数据解耦；
- **阶段三（P2 进阶优化）**：热/冷列物理分离 + 增量 Checkpoint + 冷热感知 Compaction + 多 SSD 条带化。
- 落地任务跟踪见 development_extension.md Ex-5（SSD 原生迁移计划）。

---

## 5. 索引设计

### 5.1 三类索引

| 索引 | 用途 | 查询方式 |
| --- | --- | --- |
| 主键索引 | `DocId` 精确定位 | LSM 有序索引 + 热点缓存 |
| 组合稀疏索引 | 固定高频条件（`a=? AND b=?`） | 一步定位，无需合并 |
| 原生倒排索引 | 任意字段自由筛选 | 单字段列表 + DocId 集合交集/并集 |

**索引存储格式：**

- **组合稀疏索引**：LSM 键为 `Encode(field1, field2, ..., DocId)`，**值为空**；利用 LSM 键顺序性支持等值快速 seek 与范围扫描；DocId 作为键后缀，同一组合值下天然有序，无需维护额外列表；
- **倒排索引**：**独立于 LSM 主数据的「内存哈希字典 + 追加式倒排文件」**（见 5.2）——Term 定位 O(1)、列表零随机写，替代"倒排存为 LSM KV"的旧思路；
- **倒排列表压缩**：统一 **RoaringBitmap**（低基数 run / 高基数 bitmap 容器）；
- **倒排列表分块**：按物理分片预分段（Chunk），广播查询直接拼接（见 5.2.1）；
- 热门 term 前缀（Top N DocId）常驻内存 Term 缓存，与 HotCache 联动（见 6.4）。

### 5.2 倒排索引：内存哈希字典 + 追加式倒排文件（读时 Hash + 写时 Append）

**为什么不能直接套用 B-Tree / 全局 Hash：**

| 方案 | 读 Term | 写入 / 更新 | 结论 |
| --- | --- | --- | --- |
| B-Tree / B+Tree | O(log N)，支持范围扫描 | 随机写 + 页分裂，写放大极高 | ❌ 丢掉高吞吐写入命根子 |
| 全局 Hash（纯内存） | O(1) 极致快 | Term 基数大（千万级）内存装不下，扩容 STW | ❌ 纯内存不可行 |
| MySQL AHI | 只缓存热点 Term | 80% 冷 Term 依旧慢 | ⚠️ 只解决热点 |
| **本方案：内存字典 + Append 文件** | O(1) 查表 + 1 次顺序读 | 追加写（顺序 IO），与 LSM 同源 | ✅ 读写双优 |

**核心架构（读写分离）：**

```
┌───────────────────────────────┐
│ 内存 Term Dictionary（热）      │  DashMap<String, TermMeta>
│  O(1) 查 (file_id, offset,     │
│       length, doc_count)       │
└──────────────┬────────────────┘
               │ 定位
┌──────────────▼────────────────┐
│ Append-Only 倒排文件（冷）      │  [Header][TermData1][TermData2]...
│  TermData = 压缩 DocId 列表     │  （RoaringBitmap）
└───────────────────────────────┘
```

- **内存结构（Term Dictionary）**：`DashMap<String, TermMeta>`（并发安全）；`TermMeta` 约 64B / Term：1.5 亿数据（设计基准）、500 万唯一 Term 仅占 **~320MB**，容量完全可控；
- **磁盘结构（Append-Only Inverted File）**：`[Header][TermData1][TermData2]...`，`TermData = [压缩后的 DocId 列表（RoaringBitmap）]`；
- **写入路径**：新文档写 LSM 主数据的同时，将其字段 Term 对应 DocId **追加到倒排文件尾部**，并更新内存 Hash 的 `(offset, length)` 指向最新位置——**零随机写**，完美保留 LSM 顺序 IO 优势；
- **读取路径**：内存 Hash O(1) 定位 → `pread()` 读该段压缩数据 → 解压 RoaringBitmap → 返回；对比原 LSM 多层二分（平均 5~7 次磁盘 IO），延迟从 5~30ms 降至 **0.5~2ms**；
- **统计载荷**：`doc_count` 内嵌于 TermMeta（常驻内存），COUNT / GROUP BY 零磁盘访问（见 7.2）；
- **追加序 = 时间序**：每个 Term 的尾部即最新 DocId，Top-N（最新 N 条）直接读 Tail，天然支持（替代原"键反转"方案）。

**TermMeta 二进制格式：**

```
TermMeta = file_id(u32) ++ offset(u64) ++ length(u32) ++ doc_count(u32) ++ last_updated_ts(u64)
```

**RoaringBitmap 压缩标准**：倒排 Body 统一 RoaringBitmap（container 级：array / bitmap / run），低基数段用 run 压缩、高基数段用 bitmap 容器。

**崩溃恢复**：追加写 + 内存更新原子化；进程崩溃后，重启通过重放 WAL（含倒排追加位置）重建内存 Hash 表，或扫描倒排文件重建。

### 5.2.1 倒排列表预分片 Chunk（为广播查询减负）

- 同一 Term 的 DocId 列表不直接存原始 DocId，而是按 `DocId` 路由归属的**物理分片**（一致性哈希，见 9.1）**物理切段（Chunk）**；
- 集群 8 分片时，`status='active'` 的列表在磁盘上拆成 8 个连续小段，每段只存属于该分片的 DocId；
- **广播查询优化**：网关只要求每个分片返回属于自己分片的那段 Chunk 的压缩 Bitmap（其他分片数据不返回）→ 网关拿到 8 个 Bitmap **按顺序直接拼接**（无需交集/并集复杂计算），CPU 合并开销从 O(N) 降为 O(1)；
- 收益：广播倒排延迟从 15~30ms 降至 3~8ms，并发能力从 1000 QPS 提升至 **>10,000 QPS**。

### 5.2.2 倒排文件 GC / Compaction

- **MVP（零成本）**：倒排文件只增不减，旧 Term 数据留在文件前面；内存 Hash 永远指向最新尾部，不影响正确性；
- **后台 GC（与 LSM 类似）**：倒排文件超过阈值（如 1GB）时，后台线程读取所有 Term 的最新 Bitmap，**重写为新的紧凑文件**，原子替换内存指针，删除旧文件；
- **崩溃一致性（段清单 Segment Manifest，必做）**：GC 不可"写完新文件直接删旧文件"——中途崩溃会丢数据。正确顺序：新段写**临时文件** → `fsync` → **原子更新 Manifest**（JSON / 二进制清单，列出全部有效段）→ 删除旧段；**重启时只加载 Manifest 中列举的段**，彻底规避半成品文件；
- 全过程顺序读 + 顺序写，不阻塞前台查询；与主数据 Compaction 共用 IO 优先级隔离（见 4.5）；生产形态升级为分层分段合并（见 5.2.4 ⑤）。

### 5.2.3 资源估算指南（以单节点 1.5 亿文档为锚点）

**字段基数假设（估算前提，必须明确）：**

| 字段类型 | 数量 | 基数（唯一值数） | 平均长度 |
| --- | --- | --- | --- |
| 高基数 ID 类（user_id, device_id） | 5 | 2 亿（几乎全唯一） | 32 字节 |
| 中基数标签类（city, app_version） | 15 | 10 万 ~ 100 万 | 16 字节 |
| 低基数枚举类（status, type, level） | 30 | 10 ~ 1000 | 8 字节 |

单节点倒排记录总数（Posting 条目）= 1.5 亿文档 × 50 字段 = **75 亿个 (Term, DocId) 对**。

**内存占用（内存哈希字典）：**

`TermMeta`（Rust struct 对齐后）约 **64B / Term**：

| 项 | 大小 |
| --- | --- |
| 键（Term 字符串，高基数拉高平均） | 20B |
| FileID (u32) | 4B |
| Offset (u64) | 8B |
| Length (u32) | 4B |
| DocCount (u32) | 4B |
| LastTS (u64) | 8B |
| 哈希表指针 / 开销（DashMap） | ~16B |

单节点唯一 Term 估算：高基数 5 字段 ≈ 1.1 亿 + 中基数 15 字段 ≈ 750 万 + 低基数 30 字段 ≈ 1.5 万 ≈ **1.18 亿**。

**内存总占用**：1.18 亿 × 64B ≈ **7.6 GB**。64GB 物理机中占 ~12%；叠加 HotCache（8GB）+ BlockCache（4GB）+ LSM 内存，总计约 30~35GB，接近红线，须预留 Page Cache。

**磁盘空间占用（倒排列表数据）：**

| 项 | 值 |
| --- | --- |
| 未压缩原始 | 75 亿 × 8B（DocId u64）= 60GB |
| 压缩后 | 差值编码 + 变长整数 + RoaringBitmap，业界（ES/Lucene）实测压缩比 20%~30% → **~15GB** |
| 备注 | Term 仅存内存字典，磁盘只存压缩 Bitmap；如需存 Term 另加 ~4GB → 总计 **~15~19GB** |

**集群汇总：** 8 节点 ≈ 12 亿文档（总磁盘 ~2.4TB、字典内存分摊 ~60GB）；32 节点 ≈ 48 亿（**50 亿级**）。

### 5.2.4 倒排索引极端防护与容灾设计

**① 魔鬼倒排列表（Super Term）→ 结果集截断 + 采样优化**

- 触发：某 Term 覆盖 90% 文档（如 `status='active'`，单列表 1.35 亿 DocId），查询返回巨大 Bitmap（网络传输 100MB+、网关 OOM），CPU 交集 >10s；
- **查询门控**：列表 `DocCount > inverted.max_posting_scan`（默认 1_000_000）时拒绝展开该列表，降级为**全表扫描 + Zone Map 过滤**（顺序 IO 反而比随机读 1.35 亿 DocId 更快）；
- **运营场景特化**：强制要求带时间范围（利用 5.4 时间分区），把扫描范围缩小到最近 1 天；
- **熔断返回（Query Too Expensive）**：多个大集 AND 且均为超大规模（如 `status` 覆盖 1 亿 + `type` 覆盖 8000 万）时，优化器判定计算成本过高，直接返回 **`Query Too Expensive`** 错误并提示补时间范围；`broadcast_query.max_concurrent`（默认 10）与 `inverted.max_posting_scan`（默认 100 万）为**必启用**熔断配置，网关层可硬拦截无时间范围的大集查询。

**② 字典扩容风暴（Resize Storm）→ 预分配 + 冻结分段字典（Frozen Segments）**

- 触发：写入高峰新 Term 激增，DashMap 全局 Rehash，写入 P99 从 1ms 飙到 100ms；
- **启动预分配**：按 `inverted.estimated_unique_terms`（默认 1.5 亿）提前 `HashMap::with_capacity()` 分配足量桶，禁止自动扩容；
- **分段字典（Cold / Hot 分离）**：
  - Hot 字典（可写）：只存最近 1 小时新 Term，容量上限 `inverted.hot_dict_max_entries`（默认 100 万），dashmap；
  - Frozen 字典（只读）：每小时将 Hot 冻结为**不可变 HashMap**（无锁、读取极快），持久化到磁盘；
  - 查询：并行查 Hot + 所有 Frozen，合并结果；
- 收益：写入永不触发 Rehash，查询无锁竞争。

**③ 冷启动穿透 → 异步 Checkpoint 快照 + 双缓冲加载**

- 触发：重启后内存字典为空，扫描 ~19GB 倒排文件重建 ~1.18 亿条 Meta 需 10~30 分钟；
- **Checkpoint**：后台每 `inverted.checkpoint_interval_secs`（默认 300s）将字典 Meta 快照为 `term_meta.snapshot`（LSM 有序格式，非哈希）；
- **恢复流程**：① 加载最新 snapshot（~4GB 压缩、纯顺序读，约 5~8s）→ ② 重放 Checkpoint 之后的增量 WAL（倒排追加日志）→ ③ 全部完成后才切换 Ready；
- **降级体验**：加载期间拒绝写入、允许只读查询（旧数据可见、新数据暂不可见），把"完全不可用"变为"部分只读"。

**④ 多重低基数 AND → 查询优化器选择最小集**

- 触发：`status=active AND level=error AND region=us` 三列表各 5000 万 DocId，直接交集 CPU O(N) 叠加 >5s；
- **代价估算**：执行 AND 前先查内存字典获取每个 Term 的 DocCount；
- **执行策略**：永远以 DocCount 最小的 Term 为基础集合（如 `level=error` 仅 100 条），展开后逐个 DocId 检查其他条件（布隆过滤器 / 位图 O(1)）；
- 结果：CPU 从 O(5000 万 × 3) 降为 O(100 × 3)，延迟从 5s → 0.5ms。

**⑤ 倒排文件 GC 写放大 → 分层分段（Tiered Segments），升级 5.2.2**

- 触发：单一大文件合并时读写 20GB，磁盘 IO 占满、前台写入超时；
- 不合并成单个大文件，维护多层段：L0（16MB）→ L1（128MB）→ L2（1GB）… 最多 5 层（`inverted.segment_max_size_mb` 可配）；
- 每次只合并**最小的 2 个段**（类 LSM leveled），单次 IO 不超过 2GB，避免 IO 尖峰；
- **原子切换**：新段写临时目录、fsync 后原子重命名替换，查询通过 `Arc<SegmentList>` 无感切换。

**⑥ 极端内存保护（OOM 终极防御）**

- 配置 `inverted.max_memory_bytes`（默认 12GB）；
- **软限流**：字典内存 > 10GB 时，新 Term 写入改**异步批处理**（攒满 1 万条才插入），降低插入频率；
- **硬拒绝**：> 12GB 返回 `507 Insufficient Storage` 拒绝新写入，直到 Compaction / GC 释放内存（兜底，极少发生）。

**配置汇总：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `inverted.max_posting_scan` | u64 | 1_000_000 | 倒排列表展开门控，超限降级全表扫描 |
| `inverted.estimated_unique_terms` | u64 | 150_000_000 | 启动预分配字典容量 |
| `inverted.hot_dict_max_entries` | u32 | 1_000_000 | Hot 字典容量上限，超限冻结 |
| `inverted.checkpoint_interval_secs` | u64 | 300 | 字典 Meta 快照周期 |
| `inverted.segment_max_size_mb` | u64 | 1024 | 倒排段最大容量，GC 分层合并 |
| `inverted.max_memory_bytes` | u64 | 12GB | 字典内存硬上限（507 拒绝） |
| `inverted.engine` | enum | "hash" | 倒排字典引擎："hash"（MVP）/ "fst"（阶段 1.5，mmap 亚秒冷启动） |

### 5.2.4.1 冷启动穿透优化：FST + Mmap（阶段 1.5 高优先级）

**原理**：将倒排字典从「内存哈希表」改为「不可变 FST + mmap」——FST（Finite State Transducer）是确定无环图，可压缩存储海量字符串 Term，支持 O(len(term)) 前缀/精确查找（Lucene 术语字典同款方案，Rust 用 `fst` crate）。

**启动魔法**：FST 编译后是纯二进制字节流；进程启动仅执行一次 `mmap("/data/term_dict.fst", PROT_READ)`，OS 只建虚拟地址映射，实际物理内存由**缺页中断按需加载**——零加载、零解析、零构建。

**对比（哈希表 vs FST + Mmap）：**

| 指标 | 哈希表（旧） | FST + Mmap（新） |
| --- | --- | --- |
| 启动耗时 | 8~15s（加载 + 解析 + 构建） | **< 50ms**（仅 mmap 系统调用） |
| 内存占用（RSS） | 固定 ~8GB（全量加载） | 按需加载：覆盖 20% 热 Term 仅 ~1.6GB，全量扫描才占满 ~8GB |
| Term 查找 | O(1) 内存操作 | O(len(term))，Page Cache 命中时速度 ≈ 内存 |
| 内存爆炸风险 | 高（须预留 ~8GB） | 低（OS Page Cache LRU 自动淘汰冷页） |

**写入维护成本（FST 不可变 → 分层 FST）：**

- **base.fst**（只读）：定期全量重建；
- **delta.fst**（增量）：新 Term 写 delta，每 5 分钟 Compaction 合并一次；查询**并行查 base + deltas**；
- **全量重建后台进行**：每 30 分钟读取最新倒排文件编译新 FST，原子重命名替换，旧文件由 OS 自动回收；
- 与 5.2.4 ③ 的 Checkpoint 快照互为补充：**MVP 用 Checkpoint（<15s），阶段 1.5 升级 FST（亚秒级）**；
- **与 Checkpoint 衔接（过渡路径）**：升级 FST 后 Checkpoint **不删除**，转为 FST 的**构建源**——后台定时将 Checkpoint 编译为 `base.fst`，两者共存、无缝替换；回滚时仍可用 Checkpoint 恢复。

**收益**：让 OS 内核管理冷热数据（ScyllaDB / ES 底层策略），RSS 基座从固定 ~8GB 降为按需加载，64GB 机器承载 50 字段 / 1.5 亿数据（设计基准）内存更从容。

### 5.3 倒排索引 vs 组合索引（选型规则）

- **倒排索引**：单字段索引，查询时对多个 DocId 列表现场做集合运算（AND=交集，OR=并集）。灵活，但长列表交集 CPU 开销高；
- **组合索引**：索引键直接合并多字段（如 `(status, type)`），查询一步到位。快，但需预先建立、组合爆炸、灵活性差。

**执行规则：**
1. 查询条件固定（经常 `a=? and b=?`）→ 用**组合稀疏索引**，省去倒排集合合并开销；
2. 查询条件不固定、字段任意搭配 → 用**单字段倒排 + 查询时 DocId 集合合并**，用少量 CPU 换取灵活性；
3. 注意：倒排"集合合并"≠ SQL 表 JOIN，是对同一份文档库的两份 DocId 列表做集合运算，无跨表。

### 5.4 时间分区策略（TTL 自动过期，阶段 1.5）

**痛点**：日志只查近 3 天，磁盘却被 3 个月前的旧数据填满；手动删数据产生大量墓碑拖垮写入。

**方案：时间窗口 SST 分区（Time-Partitioned SST）——删除成本 O(1)。**

- **按时间分桶**：SSTable 按文档 `timestamp` 字段路由写入对应时间桶目录（如按天 `/data/2026-08-26/`）；
- **桶内 Compaction**：Compaction 只在桶内进行，跨桶不合并；
- **过期删除**：后台线程检查——某天桶时间 < `now - ttl_days`，直接**删除整个目录**；
- **收益**：删除成本从 O(文档数) 降为 O(1)（删目录），**不产生墓碑**，彻底杜绝旧数据拖垮 Compaction；
- **配置**：`storage.ttl_days`（0 表示关闭）、`storage.time_bucket`（"day" / "hour"）。

### 5.5 二期增强

- 位图索引 BitMap：适合枚举、离散值字段（设备编号、状态码、标签），作为**倒排索引的增强**（term → 内存 Roaring Bitmap），查询远快于 B 树；**MVP 不实现**，仅预留设计空间；
- 查询结果缓存：组合条件 → DocId 列表，配 TTL 或基于写失效（缓存粒度精确到具体条件）；**MVP 不做，列为二期优化**；
- 复合倒排（预先合并多字段 term）：**不推荐**，term 数量爆炸且灵活性丢失，ES/Lucene 也几乎不用。

---

## 6. 热点缓存 HotCache（性价比最高的优化）

### 6.1 结构与定位

```
struct HotCache {
    map: DashMap<u64, Document>,   // 并发安全哈希表，原理同 Redis dict
    heat: DashMap<u64, u32>,       // 热度计数器
    max_bytes: usize,              // 内存硬上限
}
```

- `DocId(u64)` 做 key，哈希查找 O(1)；
- **缓存 = 加速副本，非权威数据源**。原始数据永远在磁盘 LSM，缓存崩溃清空后系统可穿透磁盘正常工作，无数据丢失风险；
- 推荐缓存**完整文档对象**，命中直接返回，零磁盘 IO。

### 6.2 内存预算与淘汰

- **硬性内存上限**（示例 16G 机器）：LSM 缓冲 8G + HotCache 4G + 其余留系统页缓存；
- 淘汰算法按推荐优先级：**LFU（热度优先，首选）> LRU-LFU 混合（工程最优）> LRU（最简单）**；
- **禁止**用固定 TTL 过期——高频热数据未到期就被踢，命中率暴跌。

### 6.3 一致性

- 写入 LSM（权威源）后**主动失效缓存旧条目**（缓存失效，而非缓存双写）；
- 下次查询未命中 → 穿透磁盘读最新版 → 重新载入缓存；
- 采用最终一致性模型，实现最简单、无双重写不一致的 bug。

### 6.4 与倒排联动（二期）

**联动策略：** 当文档进入 HotCache 时，将该文档各字段值对应倒排列表的 **Top N 个 DocId**（默认 N=100，与 6.6 预热阈值一致）预加载到内存 Term 缓存：

1. 每个 term 仅缓存倒排列表的**前 N 个 DocId 片段**（而非全量），控制内存占用；
2. Term 缓存按访问热度维护，冷 term 的缓存片段自动淘汰；
3. 效果：文档晋升后，其关联条件查询可先命中 Term 缓存中的热点片段，主键查询与条件检索双重加速。

### 6.5 缓存配置模型（全配置项可调，纳入 MVP）

**HotCache（文档缓存）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `hotcache.enabled` | bool | true | 总开关 |
| `hotcache.max_memory_mb` | usize | 4096 | 硬上限，超出触发淘汰 |
| `hotcache.initial_capacity_mb` | usize | 256 | 预分配哈希表容量，避免扩容抖动 |
| `hotcache.eviction_policy` | enum | "lfu" | "lru" / "lfu" / "tiny-lfu"（Caffeine 算法） |
| `hotcache.eviction_high_water` | f64 | 0.85 | 软水位，内存达此比例主动淘汰冷数据（防突发卡顿） |
| `hotcache.eviction_low_water` | f64 | 0.75 | 淘汰至该水位即止 |
| `hotcache.hot_threshold` | u32 | 5 | 每秒访问几次算"热"，触发联动预热 |
| `hotcache.prewarm_on_startup` | bool | false | 是否从持久化热点统计文件恢复 |
| `hotcache.max_document_size_bytes` | usize | 102400 | 超过此大小的文档不缓存，防止大对象挤占内存 |

**BlockCache（SSTable 数据块缓存）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `blockcache.max_memory_mb` | usize | 2048 | 与 HotCache 隔离，防止相互挤占 |
| `blockcache.block_size_kb` | usize | 16 | SST 数据块大小；机械盘选 64KB，NVMe 选 16KB |
| `blockcache.eviction_high_water` | f64 | 0.85 | 软水位，内存达此比例主动淘汰旧块 |

> **说明**：以上为**保守出厂默认值**；64GB 机器建议按 5.2.3 预算上调——HotCache 8GB + BlockCache 4GB + TermCache 2GB（与选型文档示例一致），并按 13 章校验规则（缓存总和 < 可用内存 × 0.7）约束。

### 6.6 缓存与倒排联动：失效链与预热算法

**写入失效链**（保证查询不读到新旧混合数据）：

```
Put(DocId) → ① 使 HotCache 中该 DocId 失效
          → ② 将该文档旧版本涉及的所有倒排 Term 缓存列表中的 DocId 标记失效（而非立即重算）
          → ③ 最后写入 LSM（权威源）
```

**预热策略（可配置）：**

- 某 DocId 在 1 秒内被查询超过 `hotcache.hot_threshold` 次（默认 5）→ 触发预热：将其字段值关联的倒排 Term 的 **Top N 个 DocId**（N 可配，默认 100）提前载入 Term 缓存；
- **启动预热**：`hotcache.prewarm_on_startup = true` 时，从磁盘持久化的热点统计文件重建缓存，避免重启后冷启动毛刺；
- **防抖动**：淘汰与预热均按热度渐进进行，禁止批量清空重建，避免缓存颠簸。

### 6.7 Compaction 缓存失效联动（防缓存污染）

**隐蔽点**：Leveled Compaction 合并 SST 会生成新数据块、删除旧 Block——BlockCache 中缓存的旧 Block 虽仍可命中，但内存无法释放直到 LRU 淘汰；且旧的"块映射关系"（DocId/BlockSize → FileID）变旧，查询优化器可能拿陈旧定位去读，引发 Cache Miss 骤增（命中率降 20%~30%）。

**根治：**

1. **失效联动信号**：Compaction 线程完成一个文件合并后，向 BlockCache 发送 `invalidate_blocks(file_id)` 信号（仅标记失效，不立即清理）；
2. **双重索引**：BlockCache 的 Key = `(SST_File_ID, Block_Offset)`；查询时先查元数据获取**最新 File_ID**，再取 Cache——老旧缓存的 Block 自然永不被命中、自动淘汰；
3. 与 6.3 写失效链互补：写路径失效按 DocId，Compaction 失效按 FileID，两条失效通道互不冲突。

---

## 7. 查询执行路径

| 场景 | 路径 | 目标延迟 |
| --- | --- | --- |
| 热点主键查询 | HotCache 命中直接返回 | 亚毫秒 |
| 冷主键查询 | 稀疏索引 → 布隆过滤 → **Zone Map 粗筛** → 块缓存/磁盘 | 0.1~1ms |
| 固定条件查询 | 组合稀疏索引一步定位 | ≈ MySQL |
| 任意字段筛选 | 内存 Hash 查 TermMeta → pread 读 Bitmap → DocId 交集/并集 → **Zone Map 粗筛** → 二次取文档 | 灵活，单机 P95 <1.5ms |
| 范围查询 | **Zone Map 剪枝** → 命中块顺序扫描（顺序 IO） | 优 |

**范围查询设计提示**：SST 文件按键有序，尽量把相近范围数据落到同一 SST，顺序读远快于随机读。

**Zone Map 粗筛步骤**（位于布隆过滤器之后、读块之前）：扫描 SST 时先用块级 min/max 过滤不满足范围条件的数据块，跳过无效读块与解压；倒排回表前同样先过 Zone Map（见 4.4.1）。

**倒排查询流程（5.2 新架构）：**

1. **内存哈希表查 TermMeta**（O(1)）；
2. 按 Meta 定位磁盘段，`pread()` 读取压缩 Bitmap 并解压；
3. 广播查询时直接**拼接各分片预分段 Chunk** 的 Bitmap（见 5.2.1），无需交集/并集复杂计算。

### 7.1 查询优化器（Lite）：多级索引路由选择（纳入 MVP）

替代硬编码固定路径，根据查询条件动态选择最优索引路径：

**决策逻辑：**

1. **主键点查**（`DocId` 精确）→ 直达 HotCache / LSM，禁用倒排；
2. **组合索引匹配**：检查条件是否完整覆盖某已建组合索引的前缀（如组合索引 `(a,b,c)`，查询 `a=1 AND b=2` 可命中）→ 走组合索引一步定位；
3. **倒排 + Zone Map 混合**：其余条件走倒排取 DocId 列表，同时对每个 SST 数据块做 Zone Map 范围剪枝——**先剪枝、再取倒排**，减少无效 term 集合运算；
4. **代价估算**（MVP 后可做）：内存维护各 term 的近似文档频率；若某条件倒排列表极长（如 `status='active'` 占 80%），**跳过倒排**，直接全表扫描 + Zone Map 过滤，避免大集合交集 CPU 爆炸。

**配置暴露：**

- `query.optimizer.enabled = true/false`：允许用户强制走指定索引，用于调试与对照压测。

### 7.2 聚合查询执行器（COUNT / GROUP BY，阶段 1.5）

- **COUNT 优化**：如 `status='active'` 直接读内存 TermMeta 的 `doc_count` 返回，耗时 < 0.1ms，无需扫描列表、无需回表；
- **GROUP BY 优化**：遍历该字段的倒排 Term 集合（内存字典 / FST），逐个取出各 Term 的 `doc_count` 直接构造分组结果，完全不需要访问文档数据；
- **SUM / AVG**（二期）：依赖值列 + 聚合表达式下推，MVP 只做 COUNT / GROUP BY；
- 与 5.2 的 TermMeta `doc_count` 配套：统计载荷常驻内存或 LRU Term 缓存，50 亿数据下 Count 不再 OOM。

### 7.3 倒排回表优化：批量预取与块排序（根治回表随机读放大）

回表随机读是倒排查询的"三步杀"：拿到 DocId 列表后逐条随机读文档，P95 高达 5~15ms。根治需三层算法配合（"排序"只是其中第一层）：

**第一层：读取重排序（Read Reorder）**

```rust
// 伪代码（Rust 风格）
async fn fetch_docs(docids: &[u64], block_size: u64) -> Vec<Document> {
    // 1. 计算 (SST_File_ID, Block_ID)，按组合键排序并分组
    let mut groups: BTreeMap<(FileId, u64), Vec<u64>> = BTreeMap::new();
    for docid in docids {
        let (fid, bid) = (locate_file(docid), docid / block_size);
        groups.entry((fid, bid)).or_default().push(docid);
    }
    // 2. 同块内 DocId 合并为一次读请求；3. 并发批量发出
    let reads: Vec<_> = groups.into_iter()
        .map(|((fid, bid), ids)| read_block(fid, bid, ids))
        .collect();
    join_all(reads).await
}
```

- 效果：1000 个 DocId 分散在 500 个块，排序分组后缩减为 ~200 个连续读请求，**IOPS 降低 60%**。

**第二层：块内预取（Prefetch）**

- 对每个需读 Block 发起**异步批量 read**（tokio::fs 或 io_uring 批量提交），而非同步循环 await；
- 充分利用 NVMe 队列深度（QD=128）：磁盘在等待第一个块时并行返回其他块，延迟重叠；
- 实测：5~15ms → **2~4ms**。

**第三层：结果集截断（Early Termination）**

- 查询带 `LIMIT 100` 且倒排返回 10000 个 DocId 时，**不先回表再排序**；按 DocId 顺序（或时间戳顺序）取前 100 个去回表；
- 回表量从 10000 降为 100，**延迟降低 99%**；依赖倒排列表按 DocId 有序（追加序 / LSM 天然有序）。

---

## 8. 备份与还原

### 8.1 MVP：冷备份 / 全量还原（必做）

| 命令 | 行为 |
| --- | --- |
| `shanshui-cunji backup /path/backup_file` | 暂停新写入 → **确保 WAL 已刷盘且所有 MemTable 已刷盘** → **主动触发倒排字典 Checkpoint** → 打包全部 SST + 倒排文件 + 字典快照 + 段清单 Manifest 为归档 → 生成元数据（文件清单、版本、时间戳、数据条数） |
| `shanshui-cunji restore /path/backup_file` | 停止服务 → 清空现有库 → 解压备份包 → **校验文件完整性与版本兼容性** → 重启加载 |

- 本质为冷备份（备份期短暂停写），边缘网关流量场景完全可接受，开发量 1~3 天；
- **一致性保证**：冷备份期间暂停写入；备份开始前强制刷盘 WAL 与所有 MemTable，保证磁盘上的 SST 集合是完整一致快照，再逐文件拷贝；
- **备份包内容**：全部 SST 文件 + **倒排文件（.inv）** + **字典快照（.snapshot）** + **段清单 Manifest** + 元数据（文件清单、当前版本号、各文件校验和）——**倒排索引独立于 LSM，遗漏则备份后倒排全丢**；

**备份文件清单（必须全量覆盖）：**

| 文件类型 | 路径 | 是否纳入 |
| --- | --- | --- |
| 主数据 SST | data/cf_data/ | ✅ |
| 组合索引 SST | data/cf_cidx/ | ✅ |
| 倒排索引 .inv 文件 | data/cf_inv/ | ✅ |
| 倒排段清单 manifest.json | data/cf_inv/ | ✅ |
| 倒排字典 Checkpoint / FST | data/snapshots/ | ✅ |
| 字段注册表 fields.idx | metadata/ | ✅ |
| 配置文件 config.toml | 根目录 | ⚠️ 可选（运维自行备份） |
- **还原流程**：停止服务 → 清空现有库 → 解压备份包 → 校验文件完整性与版本兼容性 → 重启加载；
- 备份包自带**版本标记**，还原程序校验版本，不兼容的备份拒绝恢复；
- 数据文件格式**向后兼容**，新版本可还原旧版本备份包。

### 8.2 二期增强（MVP 砍掉）

- 热备份（不停写）、增量/差异备份、定时自动备份；
- 备份加密、完整性校验、上传云端；
- CSV 批量导入导出（`shanshui-cunji-import`），作为异构迁移的过渡方案；
- MySQL 数据迁移来源（`shanshui-cunji-migrate`，见第 15 章——仅作导入源，**非协议兼容**）。

> 注意区分：**备份还原 = 本地数据安全；云边同步 = 边缘→云端数据流转**，两者分开设计，不混在一起。

---

## 9. 分布式设计（第 2 阶段）

### 9.0 分布式设计边界：我们做"集群"，不做"分布式数据库"

shanshui-cunji 的分布式设计遵循 **"计算下沉，分片自治"** 原则：

1. **分片即单机**：每个分片节点是一个完全独立的单机 shanshui-cunji 实例，拥有自己的 LSM 引擎、缓存和索引；
2. **网关无状态**：网关（Proxy）仅负责 `DocId` 路由转发、结果集拼接（DocId 层面）和熔断限流，**绝不执行跨分片的数据聚合或事务**；
3. **扩容无损**：基于虚拟分片 + 双写切换协议（见 9.1.1），保证扩容期间业务零感知、数据零丢失；
4. **明确排除**：本设计**不实现**分布式事务（2PC）、分布式全局锁、跨分片 JOIN、全局快照读（MVCC 仅在单机内有效）。相关需求应由业务层（如 Redis 分布式锁）或上游关系型数据库（MySQL / Redis）承担。

**为什么不需要 NewSQL：**

| 维度 | A 类：分布式扩展集群（shanshui-cunji） | B 类：分布式事务数据库（TiDB / Spanner） |
| --- | --- | --- |
| 核心目标 | 存更多数据（Scale-out）、扛更高吞吐 | 跨机房强一致 + 跨行事务（ACID） |
| 分片路由 | 固定哈希，单次查询只落一个分片 | 任意字段全局索引，查询可能落多个分片 |
| 跨分片事务 | ❌ 不支持（设计哲学明确放弃） | ✅ 2PC + Paxos/Raft |
| 全局一致性读 | ❌ 最终一致性（异步复制，毫秒级） | ✅ 线性一致性（TrueTime / TSO） |
| 跨分片 JOIN | ❌ 不支持（网关只合 DocId） | ✅ 分布式执行器 |
| 实现复杂度 | 低（复用单机内核） | 极高（SQL 优化器 + 分布式事务管理器） |

**给用户的承诺**："如果你需要多行事务，请把事务放在 MySQL / Redis（业务层）处理；shanshui-cunji 只做**单分片内的高速读写**。换来的收益是：8 节点集群写入总吞吐突破 **200 万 TPS**、延迟仅 1ms（TiDB 跨分片事务延迟 8ms 以上）。"

### 9.1 分片路由

- 分片键固定为 `DocId (u64)`，路由：**一致性哈希**——`DocId` 经哈希映射到 1024 个虚拟分片（`virtual_shard_id = DocId % 1024`），再映射物理节点（`sharding.consistent_hash = true`，见 9.8；简单原型可用直接取模）；
- 写入 / 主键查询：直接算出唯一分片，**单分片操作，无广播开销，延迟≈单机**；
- **平滑扩容用虚拟分片**：设 1024 个虚拟分片，映射到物理机；扩容时只迁移部分虚拟分片，双写切换，业务不停机。

#### 9.1.1 无损扩容：双写 + 秒级切换协议

扩容不能只有"双写切换"四个字，必须三步协议保证不丢数据：

**阶段一：双写（Shadow Writes）**
- 新节点（Node-B）加入集群，暂不接收读流量；
- 网关对**属于迁移分片的 DocId**，同时写入老节点（Node-A）与新节点（Node-B）。

**阶段二：数据追平（Delta Catch-up）**
- 后台将 Node-A 的全量 SST 拷贝到 Node-B；
- 拷贝完成后，Node-B 持续回放迁移期间的 WAL 增量，直到与 Node-A 完全一致（延迟 < 1s）。

**阶段三：原子切换（Atomic Switch）**
- 网关在 1 秒内（一个 TTL 心跳周期）将所有路由映射切换为 Node-B；
- 切换完成后，关闭 Node-A 对该分片的双写；
- **回滚预案**：若 Node-B 启动失败，网关在 5 秒内自动切回 Node-A（旧数据完好无损）。

**收益**：扩容期间业务零感知、数据零丢失（No Data Loss Resharding）。

### 9.2 两类查询分流（最关键）

- **路由查询（携带分片键）**：单点下发，性能 ≈ 单机；
- **广播查询（不带分片键的倒排检索）**：网关下发给全部分片，每分片本地执行倒排算出本机 DocId 列表，网关只合并 DocId 集合（交集/并集），再二次路由取完整文档；配合 5.2.1 预分片 Chunk，各分片只返回属于本片的那段 Bitmap，网关**按序直拼（O(1)）**。

**分片内索引设计：**

- 分片后每个分片**独立维护本地索引**：组合稀疏索引、倒排索引均在分片内生效，索引与主数据同列族存储，不跨片；
- 广播查询时网关**只合并 DocId 列表**（交集/并集），不跨分片读取完整文档；
- 二次路由取文档时按 `DocId` 一致性哈希（虚拟分片）定位到对应分片读取；扩容迁移期间由元数据中心维护分片映射，保证路由一致性。

### 9.3 高可用（可选开启）

- 模型：一主多从；写入只发主节点，**异步复制**到副本（最终一致性）；
- 主节点宕机：元数据中心触发切换，副本升级为主，网关路由更新；
- **复制模式（`replication.sync_mode` 可配置）**：
  - `async`（默认，高性能）：写入延迟 ≈ 单机，无 Raft 三副本同步的 3~8ms 开销；代价是极端宕机极小概率丢少量最新数据；
  - `sync`（强一致）：等待 Slave ACK（`replication.ack_timeout_ms`，默认 1000ms）后再返回，写入延迟上升至与 Raft 三副本同量级；
  - **典型场景推荐**：边缘网关用 `async`（吞吐优先）；金融等强一致边缘场景用 `sync`（可牺牲部分吞吐）。

### 9.4 网关禁止做的事

- 禁止跨分片事务、跨分片 JOIN、分布式锁；
- 不做重型大结果集内存排序；
- 不跨分片读完整文档（只合并 DocId）。

### 9.5 性能目标（16 核 / 64G / NVMe / 1.5 亿文档基准）

> **测试基准**（测试时补充）：16 核 / 64G 内存 / NVMe SSD 单节点；数据集 **1.5 亿文档 / 50 字段**（对齐 5.2.3 设计基准），单文档约 1KB；写入线程 8、查询线程 16；数据分布满足 zipf 热点偏斜。
>
> **指标口径**：以上性能指标在「1.5 亿文档 / 50 字段、倒排字典内存约 8GB」负载下测得，网络层基于**协程（异步）运行时**；极端热点条件（Super Term、超大 AND 集合）受倒排门控、最小集枚举与熔断保护（见 5.2.4 / 7.1），延迟可预测。
>
> **异步协程收益**：网络收包 / 解析 / 路由不再占用 OS 线程资源——10k 长连接下 P99 仅增加 ~5%（同步模型会飙升 300%）；瓶颈从"网络线程切换"下沉到**磁盘 IOPS 与 CPU 位图计算**。

| 操作类型 | 连接数 | P95 延迟 | 极限吞吐 | 瓶颈层 |
| :--- | :--- | :--- | :--- | :--- |
| 单条写入（组提交） | 100 | 0.9ms | 22 万 TPS | WAL 磁盘 fsync |
| 单条写入（高并发） | 10,000 | 1.2ms | 22 万 TPS | 网卡带宽（协程无瓶颈） |
| 热点主键查询 | 100 | 0.15ms | 85 万 QPS | 内存带宽 / 序列化 |
| 热点主键查询 | 10,000 | 0.3ms | 85 万 QPS | 网卡带宽 |
| 倒排单 Term（中集 100 万条） | 100 | 1.8ms | 4 万 QPS | CPU（RoaringBitmap 解压） |
| 倒排 AND（小集枚举） | 100 | 1.0ms | 8 万 QPS | 计算池线程调度 |
| COUNT / GROUP BY 聚合 | 任意 | **0.08ms** | > 100 万 QPS | 内存查表（无磁盘 / 无回表） |
| 全集群广播倒排 | 10 | P95 < 8ms | > 10,000 QPS | 预分片 Chunk 直拼（5.2.1） |

对比：TiDB 强一致三副本写入 3~8ms → 本架构单点写入大幅领先。

### 9.6 容量

- 单节点设计基准：**1.5 亿文档 / 300GB 磁盘**（写入 P99 < 2ms、热点查询亚毫秒）；
- 集群线性扩展：8 节点 ≈ 12 亿；16 节点 ≈ 24 亿；32 节点 ≈ 48 亿（**50 亿级**）；可持续横向扩容。

### 9.7 业务侧使用约束

1. 优先带 `DocId` 查询，享受单分片单机级速度；
2. 不带分片键的倒排检索是广播慢查询，勿放超高并发核心路径；
3. 不做跨分片强关联查询、不用跨分片事务；
4. 高频固定多字段查询，在分片内建组合稀疏索引，不走倒排求交集；
5. **运营分析类查询强制约束**：不带时间范围的超大结果集筛选将触发熔断（`Query Too Expensive`，见 5.2.4 ①）；运营后台应强制带 `time_range`，利用 TTL 时间分区（5.4）把扫描范围控制在 1~2 天内，获得毫秒级响应。

### 9.8 分布式配置清单（阶段 2 全部可配置）

**运行模式：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `server.mode` | enum | "standalone" | "standalone" / "cluster" |
| `cluster.node_id` | string | "node-1" | 集群唯一标识 |
| `cluster.listen_addr` | string | "0.0.0.0:8080" | 对外服务端口（HTTP/TCP） |
| `cluster.internal_rpc_port` | u16 | 9090 | 分片节点间内部 RPC（数据同步、心跳） |

**分片路由（启用集群时）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `sharding.enabled` | bool | false | 是否开启分片（单机模式强制 false） |
| `sharding.total_shards` | u32 | 0 | 物理分片总数（不可变，扩容用虚拟分片） |
| `sharding.virtual_shards` | u32 | 1024 | 虚拟分片数（推荐 1024/2048），扩容只迁移部分 |
| `sharding.shard_key` | string | "docid" | 固定为 DocId，暂不支持自定义（留扩展） |
| `sharding.consistent_hash` | bool | true | 一致性哈希 / 取模；推荐一致性哈希减少扩容抖动 |

**主从与副本（高可用）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `replication.enabled` | bool | false | 是否开启副本（单机 false） |
| `replication.role` | enum | "master" | "master" / "slave" |
| `replication.master_addr` | string | "" | Slave 填写 Master 的 RPC 地址 |
| `replication.sync_mode` | enum | "async" | "async"（默认）/ "sync"（强一致，写延迟升高） |
| `replication.ack_timeout_ms` | u64 | 1000 | sync 模式等待 Slave ACK 超时 |
| `replication.batch_size` | usize | 1000 | 异步复制攒批发送条数 |
| `replication.heartbeat_interval_sec` | u64 | 5 | 主从心跳间隔，用于探活 |

**读写分离（查询路由）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `read_write_separation.enabled` | bool | false | 是否开启读写分离 |
| `read_write_separation.read_from_replica` | bool | false | true 时普通查询（非主键点查）优先路由 Slave |
| `read_write_separation.replica_lag_threshold_sec` | u64 | 10 | Slave 延迟超此秒数则降级读 Master，防止读到旧数据 |
| `read_write_separation.force_master_for_primary_get` | bool | true | 主键点查永远走 Master（或允许走 Slave 换最终一致性） |

**广播查询熔断（关键保护）：**

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `broadcast_query.max_concurrent` | usize | 10 | 同时进行广播检索的最大并发数，防止打爆集群 |
| `broadcast_query.timeout_ms` | u64 | 30000 | 单次广播查询最大等待时间 |
| `broadcast_query.reject_without_shard_key` | bool | false | true 时拒绝不带 DocId 的查询（纯主键场景） |

### 9.9 全局 Term 缓存（网关层，阶段 2）—— 缓解广播查询并发弱

**痛点**：高频筛选条件（如 `status='active'` 命中 80% 数据）每次广播查询都要合并 8 个分片几百万 DocId，CPU 爆炸，并发一高就熔断限流。

**方案：网关层全局 LRU 缓存（Global Term Cache）。**

- **Key** = `分片 ID + Term`，**Value** = 压缩 DocId 列表（RoaringBitmap）；
- **命中逻辑**：网关收到查询先问缓存，命中直接返回该分片 DocId 列表，**不透传到后端分片节点**；
- **失效（写计数心跳，G5）**：写入路径维护 **Term 写入量计数**——某 Term 在 1 秒内写入超过阈值（默认 100 次）→ 主动通知网关失效该 Term 的全局缓存；并叠加短 TTL（默认 5s）兜底过期，双保险防脏读；
- **收益**：高频条件广播查询从"打满全部分片"变为"网关缓存直出"，并发能力数量级提升；
- 与 5.2.1 预分片 Chunk 配合：缓存命中直出本片 Bitmap，广播合并进一步降为 O(1) 拼接；
- 配置复用 `broadcast_query.max_concurrent` / `timeout_ms`（见 9.8）。

### 9.10 术语字典热备与快速恢复（TDS，阶段 2）

**设计思想**：数据节点重启时不自己重建字典，向轻量级**术语字典缓存服务（Term Dictionary Service, TDS）**拉取序列化字典快照，把"单机重建负担"转移为"集群协调"。

- **TDS 部署**：2~3 副本（复用 Meta-Server 机器，64GB 内存），专门存储所有分片的最新字典快照（bincode 序列化）;
- **节点重启流程**：数据节点启动 → 向 TDS 请求本分片字典（返回内存字节流）→ `from_bytes()` 直接重建内存字典（或直接 mmap 挂载）→ 纯内存反序列化、无磁盘 IO；耗时 ≈ 网络传输 ~8GB（万兆 ~7s）+ 反序列化 3~5s ≈ **10~12s 恢复**（优于本地重建）；
- **预加载 + 蓝绿切换（质变）**：TDS 独立，可在业务低谷期主动把字典**预推送**给待重启节点（重启前已完成内存预热）→ 滚动升级先启动新节点（预取字典）再切流量，**客户端无感知、无冷启动慢查询**；
- **评价**：把问题从"单机存储"转移到"集群协调"，工程量中高但架构干净，适合阶段 2 分布式上线后实施；与单机 FST + Mmap（5.2.4.1）可叠加，单机亚秒 + 集群 0 抖动。

---

## 10. 开发路线图

### 阶段 1：单机 MVP（1 个月）

| 周 | 任务 |
| --- | --- |
| 第 1 周 | 架构定稿：存储引擎方案（LSM）、数据文件/WAL 格式、网络 API、数据模型、**字段注册表（schema）**、技术文档、AI 辅助骨架代码 |
| 第 2 周 | 内核存储层：内存结构、磁盘读写、索引、持久化、WAL、双 MemTable、块级 Zone Maps |
| 第 3 周 | 网络服务 + 客户端接口 + CLI：TCP/HTTP 跑通增删查改 Demo |
| 第 4 周 | 单元测试、简单压测、BUG 修复、musl 交叉编译、Nova OS 移植验证 |

**MVP 必做清单：**
- [x] Rust 编写核心存储引擎（静态 musl 兼容，后期可移植进 Nova OS）
- [x] LSM-Tree：WAL + MemTable(跳表) + SSTable
- [x] 基础 CRUD + 主键范围查询
- [x] **字段注册表持久化（schema）**：字段名 ↔ u16 映射落盘 + 自动扩展（D1）
- [x] **Varint + Delta 编码**（DocId/长度变长编码 + 有序键差值，存储缩小 30%~50%，见 4.4.2）
- [x] 磁盘持久化 + 崩溃安全（WAL）
- [x] **布隆过滤器**（主键 / 组合索引 / 倒排 term 分别生成）
- [x] **块级 Zone Maps**（Block Index 附带 min/max/null 统计，查询粗筛剪枝）
- [x] **列族支持**（主数据 / 组合索引 / 倒排索引分离，独立 MemTable / SSTable / 缓存）
- [x] **倒排索引基础**（内存字典 + Append 文件骨架 + RoaringBitmap 压缩）
- [x] **LRU 数据块缓存**
- [x] **缓存配置模型**（HotCache / BlockCache 全配置项可调，见 6.5）
- [x] **查询优化器 Lite**（多级索引路由选择，`query.optimizer.enabled` 可开关）
- [x] **看门狗（MVP 子集）**：OOM 内存限流（软/硬水位线）+ 查询超时熔断（deadline / Semaphore），防 90% 线上故障
- [x] **Tombstone 墓碑删除**
- [x] **冷备份 / 全量还原（本地文件）**
- [x] TCP 简易服务 + HTTP-JSON 接口
- [x] CLI 客户端 + 基础配置文件 + 日志输出

**MVP 坚决砍掉：**
- 多节点集群 / 分片 / 副本同步
- 复杂 SQL 解析器（先做简单自定义查询协议）
- 完整大事务
- 高级运维面板（只做最简命令行客户端）
- 容器 / 云边同步高级功能

### 阶段 1.5：列式优化（MVP 之后第一个性能冲刺）

- **PAX 混合列式数据块**：SST 数据块内按列分组（热列组头部 + 冷列组尾部）+ 列偏移量表，宽表点查只读列切片；重写 SST 格式，需保持向后兼容（新旧文件可共存）；
- **块级 Zone Maps 强化**：范围条件剪枝继续补充字段采样统计，与倒排互补；
- **TTL 时间分区（必做）**：按天分桶 SST + 过期直接删目录（5.4），删除成本 O(1)、不产生墓碑；
- **部分字段更新（必做）**：Delta CF + Merge-on-Read（4.7），写入 IO 放大从 10 倍降为 1 倍；
- **倒排统计载荷（必做）**：TermMeta 内嵌 `doc_count` + 聚合执行器（5.2 / 7.2），COUNT / GROUP BY 毫秒级、无需回表；
- **FST + Mmap 字典（高优先级，5.2.4.1）**：倒排字典从哈希表改为分层 FST + mmap，冷启动亚秒级、RSS 按需加载；
- **迁移工具基础版（G2）**：`shanshui-cunji-migrate` 支持 mysqldump 全量导入（字段映射由配置文件静态定义），降低前期试用门槛；增量 / JDBC 实时同步留阶段 3；
- **块级压缩（Zstd）+ 分区布隆过滤器（4.4.2）**：存储再降 40%~60%、布隆内存减半；
- 收益：读放大降低 90%，单机 QPS 提升 2~3 倍，宽表 P95 显著下降。

### 阶段 2：分布式集群

1. **倒排架构升级（必做，见 5.2）**：废除"倒排存为 LSM KV"，改为**内存哈希字典 + 追加式倒排文件 + 预分片 Chunk + 文件 GC**，倒排点查 P95 < 1.5ms、广播 > 10,000 QPS；
2. **分布式全配置清单（9.8）**：分片 / 主从 / 读写分离 / 广播熔断全部可配置；
3. 单机内核加网络 RPC 接口（分片节点）；
4. 轻量网关层 + 元数据中心：DocId 一致性哈希路由（虚拟分片）、单点分发（先不做广播）；
5. 广播检索、虚拟分片平滑扩容、一主多从副本高可用；
6. **看门狗补全**：写停滞假死检测自愈（14.2）+ Sidecar 进程探针（14.4）；
7. **网关全局 Term 缓存（9.9）**：广播查询结果集缓存直出，缓解并发压力；
8. **术语字典热备 TDS（9.10）**：字典快照预推送 + 蓝绿切换，重启 0 抖动；
9. **两级索引（4.4.2）**：内存索引减少 90%，更多内存留给 HotCache；
10. **外部缓存集成（21）**：Redis L2 分布式热读缓存（Cache-Aside + Write-Invalidate + 熔断降级），`[cache.external]` 默认关闭，详见 redis-integration-guide.md。

### 阶段 3：深度优化

- Leveled-Compaction 完善、压实抖动优化；
- **io_uring + 环形 WAL 高性能写入模式**（可选，默认传统 fsync）：异步提交、O_DIRECT 刷盘，NVMe 下写入延迟降低 30%~50%；
- **IO 优先级调度器**：Compaction ionice 最低优先级 + `compaction.rate_limit_mb/s` 限速 + 写 Stall 阈值精细化调优；
- **配置热加载**：SIGHUP 动态重载部分配置、环境变量覆盖完善；
- 位图索引 / 二级索引高级方案、MVCC 快照读；
- 热点 key 探测与自动缓存策略、SST 块压缩（zstd/snappy）；
- 热备份、增量备份、CSV 导入导出、**MySQL 迁移工具高级功能（增量 / JDBC 实时同步）**、异构迁移工具；
- **存算分离（远期蓝图）**：倒排索引外置 Indexer Node（独立索引节点 + WAL 重放兜底），适合百亿级规模；50 亿数据量属过度设计，不推荐 MVP 采用（详见 9.10 评价）；
- **KV 分离存储 + Ribbon Filter（4.4.2，可选）**：写放大降低 50%~80%（WiscKey 式 Value Log）；Ribbon 布隆更快更省内存；
- **前沿演进调研（22）**：YCSB 压测定位瓶颈；GPU 加速 Compaction、PMEM WAL、Learned Index、AuraDB（WAL-time KV 分离 / RL-driven Compaction）评估；CXL 长期跟踪。

### 与 Nova OS 协同

1. 开发期 NovaDB 跑在 x86 Linux 调试，不天天在 Nova OS 硬件上调试（交叉编译坑多）；
2. MVP 完成后交叉编译 musl 版本部署到 Nova OS 联调；
3. Nova OS 进入维护/小迭代模式，只补必备基础组件（SSH、进程守护、OTA、NTP），人力重心迁移到 shanshui-cunji。

---

## 11. 风险与原则

1. **不要全盘交给 AI 生成核心存储引擎代码**：存储引擎一行隐藏 bug 可能高并发运行几个月后才爆发。并发、锁、内存、文件 IO 逻辑必须人工逐行审查；
2. **心态预期**：1 个月产出的是**演示原型**，不是商用生产版本；商用还需后续 2~6 个月稳定性打磨、压测、性能优化；
3. **先正确后性能**：MVP 优先保证功能正确与数据格式稳定，跑通 写入-查询-备份-还原 全链路后，再逐个叠加算法优化，每次优化后压测对比。

---

## 12. 演进路线（做完 MVP 之后）

1. 集成进 Nova OS，做云-边演示 Demo（边缘网关采集 → 存入 shanshui-cunji → 云端网页查看）；
2. 开源发布单机版，参加竞赛；
3. 性能优化迭代 → 分布式集群版 → 企业版功能。

---

## 13. 配置体系（神经系统）与加载策略

> 数据结构是"肌肉"，集成调度算法是"大脑"，配置体系是"神经系统"。单机 MVP 可硬编码，但分布式与性能调优阶段必须全部暴露配置。

**配置加载策略：**

1. **动态热加载（二期）**：部分配置（如 `broadcast_query.max_concurrent`、`hotcache.max_memory_mb`）支持 SIGHUP 信号重载，无需重启；
2. **启动配置校验**：检查 `hotcache.max_memory_mb + blockcache.max_memory_mb < 系统可用内存 × 0.7`，否则告警并自动降级；
3. **环境变量覆盖**：支持 `shanshui-cunji__HOTCACHE__MAX_MEMORY_MB=2048` 覆盖文件配置，便于容器化部署；
4. **默认值原则**：所有配置带安全默认值、出厂即用；MVP 可硬编码实现，但配置骨架在 MVP 阶段就定义好。

**落地阶段：**

- **MVP**：缓存配置模型（6.5）+ 查询优化器 Lite（7.1）+ 写 Stall 阈值——实现代价低、收益大；
- **阶段 2**：分布式全配置清单（9.8，分片 / 主从 / 读写分离 / 广播熔断）；
- **阶段 3**：IO 优先级调度器（ionice / `compaction.rate_limit_mb/s` / 写 Stall 精细化）、配置热加载（SIGHUP）、环境变量覆盖完善。

**多核分配与 CPU 亲和性（阶段 3）：**

- **防超售（Oversubscription）**：三池若都默认抢满核，64 核机器上 Tokio 抢 64 核 + 计算池再抢 64 核 → 上下文切换风暴、性能反降。必须按负载特征**分区分配**（物理核数 N，排除超线程）：

| 池 | 分配公式 | 说明 |
| --- | --- | --- |
| 网络协程池（Tokio） | N × 0.5 或 N − 4 | 重点保障网络调度 |
| 计算池（Rayon） | N × 0.3 | 保障位图 / 聚合吞吐 |
| 后台 IO 池 | 固定 2~4 | 磁盘并发度有限 |
| 系统预留 | 剩余 ~20% | 留给 OS 与中断处理 |

- **场景化推荐（16 核）**：极致写入 12/2/2；极致查询 6/8/2；混合均衡（默认）8/6/2；4 核边缘盒 2/1/1；
- **CPU 绑核（Affinity，默认关闭）**：通过 `[affinity]` 将各池绑定到物理核心（`core_affinity` / taskset），隔离 L3 缓存、避免跨核切换毛刺（P99 稳定）；**跳过超线程虚拟核**；跨 NUMA 时避免跨插槽访存（网络池绑 CPU-0、计算池绑 CPU-1）：

```toml
[runtime]
cpu_cores_total = 0      # 0 = 自动检测物理核数，按公式分配
async_worker_threads = 0 # 建议 N - 4
compute_pool_size = 0    # 建议 N / 2
io_background_threads = 2

[affinity]               # 阶段 3，默认关闭
enabled = false
network_cpus = []        # 示例 [0,1,2,3,4,5,6,7]
compute_cpus = []        # 示例 [8,9,10,11]
io_cpus = []             # 示例 [12,13]
```

- **自适应**：`cpu_cores_total = 0` 时读取 `/proc/cpuinfo` 物理核数并按公式自动分配，开箱即用。

---

## 14. 稳定性保障体系（自愈与看门狗）

> 软件"看门狗" ≠ systemd 的 `Restart=always`，而是一套**故障预防 → 检测 → 自愈 → 降级**的闭环系统。**内存限流 + 查询超时纳入 MVP**（实现成本极低，可防 90% 线上故障）；写停滞自愈与 Sidecar 探针在阶段 2 补齐。

### 14.0 全局分配器策略（高并发关键，阶段 1.5）

> **musl 支持多线程 ≠ musl 的 malloc 适合高并发**：pthread 层完全可用，瓶颈在 malloc/free 堆分配——musl 默认分配器**整个进程只有一把全局互斥锁**，并发 alloc/dealloc 排队串行，极端场景吞吐差 2~7 倍。

数据库是典型的高频小块分配大户（每次 put/get 的 JSON 序列化、MemTable 写入、SST 解压 buffer、倒排 posting、HTTP 请求对象），且阶段 2 分布式多线程并行——**必须替换全局分配器**：

| 分配器 | 默认 | 说明 |
| --- | --- | --- |
| **mimalloc**（`mimalloc` crate） | ✅ 默认 | 轻量（边缘设备内存友好）、高并发表现好、编译快；`#[global_allocator]` 声明无 unsafe（unsafe 实现在 crate 内部，不违反零 unsafe 承诺） |
| **tikv-jemallocator**（feature `alloc-jemalloc`） | 可选 | 提供 mallctl `epoch + arena.purge` 主动归还 RSS（14.1.1 紧急止损）、stats 上报；编译较重，**建议 Linux / musl 目标使用**（Windows 需 MSVC 工具链，gnu 交叉编译受限） |

**适用场景判断**：
- 低并发 CLI / 边缘嵌入式 → 系统分配器即可，但统一走 mimalloc 也无副作用（体积/内存开销小）；
- 高并发 Tokio-web / 网关 / 消息处理 → **必须 mimalloc / jemalloc**，否则 musl 部署撞上 malloc 锁天花板；
- 长生命周期 7×24 服务 → mimalloc 碎片控制优于 musl 原生。

**注意**：`#[global_allocator]` 为编译期决定、不可运行时切换；两套 allocator 通过 feature 互斥，默认 mimalloc。

### 14.1 内存安全看门狗（OOM Guardian）—— 预防层

- **风险**：HotCache + BlockCache 内存超用触发 OS OOM Killer，进程直接消失；
- **算法**（每 100ms 检查一次当前 RSS 内存）：
  - RSS > `memory.watermark_high`（默认 0.85）× `max_memory_limit` → **软限流**：新写入的 Group Commit 主动 sleep 1ms，减缓内存增速；
  - RSS > `memory.watermark_stall`（默认 1.0）× `max_memory_limit` → **硬限流**：拒绝新写入，返回 `503 Memory Overload`，直到 Compaction 释放内存；
- **实现**：Rust 侧读取分配器统计（mimalloc 常规 stats；jemalloc 经 mallctl，比 OS RSS 更精准）；
- **配置**：`memory.watermark_high = 0.85`、`memory.watermark_stall = 1.0`。

#### 14.1.1 多层内存主动回收与紧急止损策略

> 卡死的本质：分配器（jemalloc）占用了物理内存（RSS），但内部碎片化严重，明明有剩余内存却分配失败，引发长 GC 或 Swap 抖动。内存管理必须分三层：**缓存层主动淘汰 → 内核层强制刷盘 → 分配器层主动归还**。

**各组件释放时机：**

| 组件 | 释放时机 | 释放动作 |
| --- | --- | --- |
| HotCache / BlockCache | 内存占用达 `max_memory_mb` 的 **85%（软水位）** | 实时驱逐：LFU/LRU 立刻淘汰最冷数据，直到降至 75% 以下（非等到 100% 才淘汰，杜绝突发卡顿） |
| MemTable（跳表） | 达 `memtable.max_size_mb`（默认 256MB） | 冻结切换：Mutable → Immutable → 立即后台刷盘 → 完成后 drop 跳表释放内存 |
| 倒排字典（TermMeta） | 达 `inverted.max_memory_bytes` 的 90% | 软限流：新 Term 写入改异步批处理（攒批）；达 100% 返回 `507` |
| SST 读取缓冲区 | 每次查询结束 | 即时 Drop（Rust ReadBuf 离开作用域即释放，不长期持有） |
| Compaction 合并 | 合并完成 | 删除旧 SST（unlink）+ 释放 BlockCache 旧 Block 元数据（6.7 失效联动） |

**紧急防御（Emergency Memory Panic，RSS 已达 100% 时）：**

1. **平滑驱逐（实时）**：缓存 85% 软水位主动淘汰，确保永不到达 100%；
2. **刷盘降级（紧急）**：RSS > 95% 时强制所有 Immutable MemTable 优先刷盘并释放内存；
3. **缓存缩容（硬止损）**：触发 Stall 限流时，同步强制将 HotCache / BlockCache 容量上限**临时降低 30%**，立即淘汰最冷数据；
4. **物理内存归还（核心）**：分配器为性能通常**不把内存页归还 OS**（RSS 只增不减）——默认 mimalloc 依赖 OS 页回收；启用 feature `alloc-jemalloc` 时（Linux/musl 推荐），紧急时显式调用 jemalloc `mallctl`：推进 `epoch` + 执行 `arena.<i>.purge`，强制将未使用内存页归还 OS，让系统内存监控看到 RSS 下降，避免 OOM Killer。

#### 14.1.2 三池隔离线程模型

> 进程模型：**单进程多线程**（绝对不采用多进程）——共享内存态的 HotCache 与倒排字典，避免进程间巨大拷贝开销。分片版无需重新设计，直接复用单机内核。

| 线程池 | 实现方式 | 核心职责 | 规格（16 核示例） |
| --- | --- | --- | --- |
| IO/查询主池（Runtime） | tokio 多线程工作窃取 | HTTP/TCP 网络收发、解析请求、主键点查与组合索引查询（无阻塞 IO 逻辑） | 默认 = 核数 16 线程 |
| 倒排/重度计算池（CPU Pool） | `spawn_blocking` 或独立 rayon 池 | RoaringBitmap 解压、AND/OR 交集、聚合计算——防止位图运算占满主池导致网络 Ping 超时 | 固定 6~8 线程，Semaphore 限流（并发度 = 核数-4） |
| 后台 IO 池（Background Pool） | 专用 std::thread（同步阻塞） | MemTable 刷盘（Flush）、Compaction 压实、SST 读写，绑定 io_uring / fsync | 固定 2~4 线程（磁盘 IO 并发数有限） |

- **写入路径**：请求由主池接收，写 WAL + 写 MemTable 微秒级，直接在主池处理；倒排查询与回表取数经 Semaphore 切换到计算池，防止阻塞主池；
- **协程 vs 多线程（选型依据）**：网络层强制用**协程**（异步 Task，M:N 用户态调度、纳秒级切换、栈几十 KB），计算与磁盘层强制用**多线程**（std::thread，1:1 内核调度、容忍阻塞）——**协程收网络包，线程干重活**，绝不一刀切；
- **配置化运行时（config.toml [runtime]，全部可调 + SIGHUP 热加载，阶段 3）**：

```toml
[runtime]
async_mode = "multi-thread"  # "multi-thread"（默认）| "current-thread"（边缘低功耗）
async_worker_threads = 0     # tokio 工作线程数，0 = 自动检测 CPU 核数
async_max_tasks = 10000      # 最大并发协程数，防止协程膨胀打爆内存
compute_pool_size = 8        # rayon / spawn_blocking，0 = CPU 核数 / 2
compute_queue_max = 1000     # 队列上限，满则降级全表扫描 + Zone Map
io_background_threads = 4    # 通常 2~4，NVMe 可适当调高
io_uring_enabled = false     # 阶段 3 开启（Linux 5.8+），协程化磁盘 IO
```

**部署场景推荐：**

| 部署场景 | async_mode | async_worker_threads | compute_pool_size | io_background_threads | 理由 |
| --- | --- | --- | --- | --- | --- |
| 高性能云服务器（64 核） | multi-thread | 32 | 16 | 6 | 网络与计算分离，互不干扰 |
| 标准开发机（16 核） | multi-thread | 8 | 6 | 3 | 平衡模式 |
| Nova OS 边缘盒子（4 核 ARM） | current-thread | 1 | 2 | 2 | 避免线程切换开销，极致省电 |
| 纯主键查询场景（无倒排） | multi-thread | 16 | 1（最小化） | 2 | 计算池闲置，核全让给网络协程 |

**防御与降级：**

- **协程限流**：`async_max_tasks` 达上限返回 **429 Too Many Requests**，而非无限生成协程撑爆内存；
- **计算池溢出保护**：`compute_queue_max` 满时，倒排查询不排队等待，立即降级为**全表扫描 + Zone Map**（牺牲一点延迟保吞吐，防雪崩）；
- **单线程模式适配**：`async_mode = "current-thread"` 时，Group Commit 与 WAL fsync 自动切**同步模式**，避免 `spawn_blocking` 频繁打断单线程事件循环导致上下文飙升；
- 代码级调度路由（`RuntimePools::execute_query` 显式分池）见 development.md 6.1；
- **分片继承**：分布式阶段每节点以**独立进程**启动，单机内核零修改复用；路由（一致性哈希，见 9.1）由网关计算，进程不知道自己是第几个分片，只接收带 DocId 的请求；32 节点时每进程数据量降到 ~470 万（1.5 亿 ÷ 32），单机内存压力更小、稳定性更好；
- 唯一新增的分布式内存组件在**网关层**（全局 Term 缓存 9.9、TDS 9.10），独立于存储进程，其内存预算 / LRU 淘汰 / 线程模型属网关模块**单独设计**，不侵入单机内核。

### 14.2 写入停滞看门狗（Write Stall Watchdog）—— 检测与自愈层（阶段 2）

- **风险**：后台 Compaction 线程卡死（死锁或磁盘坏道）→ L0 堆积 → MemTable 无法刷盘 → 写入完全阻塞；
- **机制**：监控 L0 文件数量变化——若 L0 数量在 `compaction.stall_timeout_secs`（默认 60s）内**无减少**，判定为 **Compaction 假死**；
- **自愈动作**：强制中断当前 Compaction 任务（丢弃未完成合并）→ 重置 Compaction 调度器 → 记录错误日志；连续失败达 `compaction.max_consecutive_failures`（默认 3）次则**主动关闭进程**（由外部 systemd / Sidecar 重启，重置文件锁）。

### 14.3 慢查询与资源隔离看门狗（Query SLA Guard）—— 降级层

- **风险**：超大范围广播倒排查询（如 `status='active'` 命中 1 亿条）耗尽 CPU 做集合交集，拖垮整个节点；
- **机制**：
  - **执行超时**：每个查询上下文携带 deadline（由 `broadcast_query.timeout_ms` 控制）；超时经 CancellationToken 触发，查询线程 `try_wait` 立即退出并返回超时错误，不额外占用 CPU；
  - **并发限流**：`tokio::Semaphore` 控制最大并发倒排合并数，超过 `broadcast_query.max_concurrent` 则排队或快速失败；
- **配置**：复用 `broadcast_query.timeout_ms` / `broadcast_query.max_concurrent`（见 9.8）。

### 14.4 内嵌 Sidecar 进程探针 —— 兜底层（阶段 2）

- 启动时 fork 一个**极轻量子进程**（或独立线程），仅负责探活：
  - 每 `sidecar.ping_interval_sec`（默认 5s）通过 Unix Domain Socket / 共享内存向主进程发 ping；
  - 连续 `sidecar.max_missed_pings`（默认 3）次无响应（主进程死锁 / panic）→ 子进程直接 SIGKILL 并重新拉起主进程；
- 比外部 systemd 感知更快（500ms 内完成自愈），作为 systemd 之上的兜底；
- MVP 实现：独立探活线程 + 文件锁心跳；阶段 2 提供独立子进程；
- **fork 安全（必读）**：在 Rust + tokio 异步运行时内直接 `fork()` 极易死锁（异步任务持有的锁被子进程继承）。**禁止在运行时初始化后 fork**；推荐用 `std::process::Command` 启动独立探针二进制（不继承锁状态），或在 tokio 运行时初始化**之前**先 fork。

### 14.5 自愈闭环与配置汇总

- 闭环：**预防（14.1）→ 检测（14.2 / 14.3）→ 自愈（14.2 / 14.4）→ 降级（14.3）**；
- 配置汇总：

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `memory.watermark_high` | f64 | 0.85 | RSS 软限流水位，触发写限流 |
| `memory.watermark_stall` | f64 | 1.0 | RSS 硬限流水位，触发 503 |
| `memory.shrink_ratio` | f64 | 0.3 | 紧急止损：缓存容量临时缩容比例（14.1.1） |
| `memtable.max_size_mb` | usize | 256 | 跳表上限，冻结切换并后台刷盘（14.1.1） |
| `compaction.stall_timeout_secs` | u64 | 60 | L0 无减少判假死超时 |
| `compaction.max_consecutive_failures` | u32 | 3 | 连续失败上限，超出主动退出 |
| `sidecar.ping_interval_sec` | u64 | 5 | 探针间隔 |
| `sidecar.max_missed_pings` | u32 | 3 | 连续丢 ping 上限，触发拉起 |

---

## 15. 迁移与兼容策略（务实路线）

**结论：绝不承诺 MySQL 协议 / 语法兼容。** MySQL 只作为**数据迁移来源**，而非协议目标。

**为什么不兼容：**

| 维度 | MySQL | shanshui-cunji |
| --- | --- | --- |
| 模型 | 关系型 + 强 Schema + 行存 + ACID 事务 | 文档型 + 无/弱 Schema + LSM 列组 + 无跨分片事务 |

- 协议兼容需实现 COM_QUERY + 完整 SQL Parser（JOIN / 子查询 / 聚合）+ 类型系统（Decimal / Date / Time）+ 事务隔离级别，至少再投入 1 年且性能大幅劣化（SQL 需翻译成 LSM + 倒排操作）；
- 宣称"兼容 MySQL"会让用户发送 `UPDATE ... WHERE id=1` 期望 ACID，结果数据不一致——灾难性口碑。

**务实的三层迁移能力：**

1. **数据迁移工具 `shanshui-cunji-migrate`**：读取 mysqldump 或经 JDBC 拉取，MySQL 每行 → shanshui-cunji 一个文档（DocId 主键）；同时将 `CREATE INDEX` 映射为组合索引 / 倒排配置；
2. **类 SQL 查询语法**：基于 `sqlparser-rs`，仅支持 `SELECT ... WHERE ... AND/OR` 子集（内部走倒排 / 组合索引）；明确不支持 JOIN / GROUP BY / 子查询 / 事务；
3. **客户端 SDK 抽象层（适配器）**：Java / Python / Go 的 `shanshui-cunjiClient`，提供与 JDBC 类似的 `query()` / `execute()` 接口，底层走 HTTP/RPC；用户 DAO 仅替换 `DataSource` 与连接字符串，SQL 改写为 Filter 语法（最小代价）。

**官方措辞标准：**

- ❌ 不写："完全兼容 MySQL，无缝切换"；
- ✅ 写法："提供 MySQL 数据一键迁移工具和类 SQL 查询语法支持，业务代码仅需修改底层驱动与连接配置即可接入"。

**给用户的预期：** 换 shanshui-cunji 是为了 10 倍写入性能与 1/10 硬件成本；代码连接层必须改，但改动的只是底层客户端，业务逻辑无需重写。

---

## 16. 用户场景覆盖与功能缺口弥补

### 16.1 性能与吞吐自评

| 维度 | 当前目标值 | 满意度 | 短板分析 |
| --- | --- | --- | --- |
| 写入吞吐 | 单机 18~25 万 TPS（协程网络） | ⭐⭐⭐⭐⭐ 远超 MySQL，媲美专业时序库 | 瓶颈下沉至 WAL fsync |
| 热点主键查询 | 亚毫秒，>80 万 QPS | ⭐⭐⭐⭐⭐ 碾压级优势 | 瓶颈在网卡带宽 |
| 带分片键条件查询 | P99 ~0.8ms | ⭐⭐⭐⭐ 与 MySQL 本地索引相当 | 组合索引需人工预建，灵活性略逊 ES |
| 任意字段倒排检索 | 单机 P95 ~1.5ms（中集 4 万 QPS）；广播 P95 < 8ms（>10,000 QPS） | ⭐⭐⭐⭐（方案落地后） | 瓶颈在 CPU 位图计算（计算池隔离，不阻塞网络） |

### 16.2 功能缺口与增强方案

| 用户需求 | 当前是否满足 | 增强方案（数据结构 / 算法） | 优先级 | 落地阶段 |
| --- | --- | --- | --- | --- |
| 海量写入（日志 / 埋点） | ✅ 满足 | 无 | - | - |
| 热点主键亚毫秒 | ✅ 满足 | 无 | - | - |
| 任意字段筛选（倒排） | ⚠️ 勉强（并发弱） | **倒排架构升级**：内存哈希字典 + Append 文件（5.2）+ 预分片 Chunk 直拼（5.2.1）+ 网关 Term 缓存（9.9） | 极高 | 阶段 2 |
| 自动清理旧数据 | ❌ 缺失 | 时间分区 SST，过期直接删目录（5.4） | 极高（否则磁盘必爆） | 阶段 1.5 |
| 修改单个字段 | ❌ 缺失 | 增量列族 Delta CF，Merge-on-Read（4.7） | 高（节省 10 倍 IO） | 阶段 1.5 |
| 统计总数 / 分组计数 | ❌ 缺失 | TermMeta 内嵌 `doc_count` 常驻内存，COUNT / GROUP BY 零磁盘（5.2 / 7.2） | 极高（运营刚需） | 阶段 1.5 |
| 查最新 N 条日志 | ❌ 缺失 | 追加序 = 时间序，读 Term 尾部即最新 N 条（5.2） | 高 | 阶段 1.5 |

**完成以上增强后**，shanshui-cunji 从"文档 KV 库"升级为**三合一引擎**：时序数据库淘汰能力（TTL 时间分区）+ 倒排聚合能力（统计载荷）+ 文档读写能力（Delta CF），覆盖「日志检索 + 实时看板 + 设备影子」三大核心场景，50 亿数据量下功能无死角。

---

## 17. 性能诊断与瓶颈定位手册（SRE 必读）

### 瓶颈矩阵总览（红绿灯表）

| 负载场景 | 主要瓶颈 | 次瓶颈 | 可扩展性 |
| --- | --- | --- | --- |
| 写入密集型（日志 / 埋点） | 🔴 磁盘 fsync IOPS | 🟡 MemTable 锁 | 横向扩容（分片），线性提升 |
| 热点主键高并发 | 🔴 网络带宽 / 序列化 | 🟢 内存带宽 | 无法无限扩展，单节点受网卡限制 |
| 复杂倒排 AND（运营后台） | 🔴 CPU 位图交集 + 回表随机读 | 🟡 网关合并 | 可扩展性差，靠缓存和剪枝优化 |
| 广播无分片键查询 | 🔴 网关 CPU + 网络带宽 | 🟡 分片节点 CPU | 中等，受网关单点限制，需做网关集群 |
| 数据量超 2 亿/节点 | 🔴 Compaction 写放大 | 🟡 磁盘空间 | 强行扩容分片，不可单机硬扛 |

### 17.1 写入路径诊断

- **监控项**：L0 文件数（触发 `storage.l0_stall_threshold` 写 Stall）、WAL fsync 耗时（组提交批大小）、Stall 事件计数（写限流 / 内存限流触发次数）；
- **常见信号**：
  - L0 持续上涨 → Compaction 跟不上：核查 `compaction.rate_limit_mb/s`、磁盘 IO、是否假死（14.2）；
  - fsync P99 偏高 → IO 调度 / 考虑阶段 3 io_uring + 环形 WAL（4.3）。

### 17.2 查询路径诊断

- **监控项**：HotCache 命中率、BlockCache 命中率、倒排位图解压耗时、回表随机读比例；
- **常见信号**：
  - HotCache 命中率低 → 调大 `hotcache.max_memory_mb` 或检查淘汰策略（6.5）；
  - 倒排解压耗时长 → 逼近 `inverted.max_posting_scan` 门控边界，评估是否改走组合索引 / 强制时间范围；
  - 回表随机读比例高 → Zone Map 剪枝（4.4.1）/ 组合索引 / PAX 列切片（1.5）。

### 17.3 分布式瓶颈诊断

- **监控项**：网关 CPU 负载、广播流量占比、Term 缓存命中率、分片节点 CPU、`broadcast_query.max_concurrent` 是否打满；
- **常见信号**：
  - 网关 CPU 打满 → 网关集群 + 全局 Term 缓存（9.9）+ 预分片 Chunk 直拼（5.2.1）；
  - 广播流量占比高 → 业务侧约束（9.7）+ 强制带时间范围（5.2.4 ①）。

### 17.4 常见瓶颈的自助调优清单（配置参数映射）

| 症状 | 优先调整参数 | 说明 |
| --- | --- | --- |
| 写入慢 / Stall 频繁 | `storage.l0_stall_threshold` ↑、`compaction.rate_limit_mb/s` ↑ | 提升 Compaction 吞吐 |
| WAL fsync 慢 | 组提交批大小、`server.wal_mode` = perf（牺牲安全） | 或阶段 3 io_uring |
| 内存吃紧 / OOM 风险 | `hotcache.max_memory_mb`、`blockcache.max_memory_mb`、`inverted.max_memory_bytes` ↓ | 启动校验 0.7 阈值（13） |
| 热点点查命中率低 | `hotcache.max_memory_mb` ↑、`hotcache.eviction_policy` | 提高缓存命中 |
| 倒排查询慢 | `inverted.engine="fst"`、`inverted.max_posting_scan` ↓ | 门控 + 亚秒冷启动（5.2.4） |
| 聚合慢 | 倒排统计载荷 `doc_count` | 阶段 1.5（7.2） |
| 广播查询超时 | `broadcast_query.max_concurrent`、`broadcast_query.timeout_ms` | + Term 缓存（阶段 2） |
| 磁盘空间紧张 | `storage.ttl_days` 开启 | TTL 时间分区整删目录（5.4） |
| 冷启动慢 | `inverted.engine="fst"`、`inverted.checkpoint_interval_secs` | FST 亚秒 / 快照恢复（5.2.4.1） |

---

## 18. 可测试性设计原则

> 良好的架构是"可测"的。shanshui-cunji 的核心模块必须通过依赖注入与 trait 抽象，支持单元测试在微秒级内完成逻辑验证，而非依赖真实磁盘 / 时钟。否则并发竞态、内存泄漏、文件损坏这类 Bug 会潜伏数月才爆发。

### 18.1 可测试性设计原则

| 原则 | 说明 | 实现方式 |
| --- | --- | --- |
| 依赖注入（DI） | 核心逻辑与外部依赖（文件系统、时钟、分配器）解耦 | trait 抽象 `FileSystem` / `Clock` / `Allocator`，生产用真实实现，测试用模拟 / 内存实现 |
| 确定性优先 | 并发竞态用确定性模型（loom）验证，而非概率性压测 | Cargo.toml `[target.'cfg(loom)'.dependencies]` 开关 |
| 状态可观测 | 核心数据结构暴露只读检查点 | `#[cfg(test)]` 下开放 pub 检查方法（`debug_check_invariants()`） |
| 时间可控 | TTL / 看门狗 / Compaction 周期使用可注入 Clock | `Clock::now()`，测试用 `MockClock` 快进 |
| 文件系统抽象 | WAL / SST / 倒排文件操作经 `FileSystem` trait 隔离 | 测试用 `InMemoryFileSystem` / `TempFileSystem` |

### 18.2 测试金字塔（单元测试占比 70%）

```
        /\
       /  \       混沌 / 端到端（5%）：真实集群、kill -9、网络分区
      /    \      集成测试（15%）：多模块协作、启动-停止-恢复
     /      \     单元测试（70%）：单函数、单模块、单数据结构
    /________\
```

- **单元测试**：每个函数 / 数据结构的正确性、边界条件、错误路径，单测耗时 < 10ms（除文件 IO 模拟外）；
- **集成测试**：CRUD 全链路、WAL 回放、备份还原、多线程竞争下的最终一致性，< 30s；
- **混沌测试**：模拟崩溃、磁盘满、Compaction 死锁、Leader 切换，< 5min。

### 18.3 三大抽象（trait）

1. **FileSystem**：隔离所有 `std::fs` 调用，支持 `InMemoryFileSystem` 与 `FaultyFileSystem` 注入；
2. **Clock**：隔离所有时间调用，支持 `MockClock` 快进（含 `now` / `sleep`）；
3. **Allocator**：隔离内存分配统计，支持 `MockAllocator` 验证缓存上限。

### 18.4 条件编译与测试辅助

- 核心数据结构（跳表、LRU）提供 `#[cfg(test)]` 下的 `debug_check` 钩子；
- `loom` 门控：依赖可替换 `std::sync` 为 `loom::sync` 的轻量包装层（`Arc` / `RwLock` 的 alias）；
- 测试辅助模块集中放在 `src/testing/`（mock_clock / mock_fs / mock_allocator / loom_wrapper），不污染生产代码。

### 18.5 验收标准

- 所有单元测试不依赖外部环境（网络、特定磁盘路径、真实时钟）；
- 并发竞态模块 100% 通过 `loom` 模型检查；
- CI 单元测试耗时 < 60s，支持每日 100 次提交级验证。

---

## 19. 关联查询（JOIN）策略：认清边界，用架构弥补

> 完整白皮书与使用指导见 [join_function.md](./join_function.md)。核心理念：**不提供 JOIN 语法，但提供 4 种高效的"数据关联"替代方案，覆盖 95% 真实需求**。

### 19.1 边界声明（为什么不做 JOIN）

- **不实现**任意多表 JOIN 语法、跨分片 JOIN（design 9.0/9.4 明确排除）、嵌套文档自动展开、外键约束 / 级联；
- **技术原因**：LSM-Tree 随机读代价极大（JOIN = 海量随机读）；倒排索引只解决单表筛选；分布式 Shuffle 放大网络开销；一个 JOIN 可吃 50% 集群 CPU，破坏运维确定性。

### 19.2 四种关联替代方案

| 方案 | 适用场景 | 阶段 | 性能 |
| --- | --- | --- | --- |
| ① 应用层二次查询 + 内存合并（SDK `queryAndJoin`） | 外键关联、结果集 < 10 万条 | 阶段 1.5 | 10 万条结果 ~16ms |
| ② 写入时预连接（Enrich 回调展开） | 关联关系固定、写入时已知 | 阶段 1.5 | 写入 +0.1~0.3ms，查询 0 增加 |
| ③ 物化视图（后台定时预聚合） | 固定维度聚合统计 | 阶段 2 | 查询 < 1ms |
| ④ 数据导出到 OLAP（`shanshui-cunji-export` → Parquet/CSV） | 复杂多表 JOIN / BI | 阶段 1.5（基础版）/ 阶段 2（增量直连） | 存储计算分离 |

### 19.3 架构支撑

- **JOIN 计划节点**（`QueryPlan::Join`）：左小→先查左再批量查右；右小→反向；两表都大→拒绝并引导导出（`join.max_rows` 默认 100 万熔断）；
- **写入路径 Enrich**：网络层接收后、WAL 写入前执行；失败策略可配（拒绝 / 降级）；数据源可配（Redis/MySQL/HTTP/本地）；
- **网关策略**：单分片内 JOIN 本地执行 ✅；跨分片 JOIN ❌ 返回错误；**小表广播 JOIN**（`join.broadcast_threshold` 默认 100 行、`join.broadcast_max_bytes` 默认 1MB）阶段 3 可选、默认关闭；
- 模块：`sdk/join.rs`、`tools/shanshui-cunji-export/`、`engine/mv_scheduler.rs`、优化器关联字段索引提示。

### 19.4 用户预期管理

> shanshui-cunji 是文档检索数据库，不提供 SQL JOIN。常见"数据关联"场景有 4 种高效替代方案；复杂多表关联分析请导出到 ClickHouse——这是"存储与计算分离"的正确姿势。

---

## 20. 运维管理接口（Admin Interface）

> 对标 MySQL 的 SHOW PROCESSLIST / SHOW STATUS / EXPLAIN 运维习惯，**选择性支持** 7 项能力：照搬事务锁机制没必要，但 DBA 最需要的"手术刀"必须有。

### 20.1 特性支持决策矩阵

| 特性 | shanshui-cunji 是否支持 | 实现方式 | 优先级 |
| --- | --- | --- | --- |
| SHOW PROCESSLIST | ✅ 必须支持 | Admin HTTP/TCP 命令 + 内部 QueryRegistry | P0（运维刚需） |
| SHOW STATUS / 内存监控 | ✅ 必须支持 | 暴露分配器 stats（mimalloc/jemalloc）+ 缓存命中率 | P0（排障刚需） |
| EXPLAIN | ✅ 必须支持（适配版） | 输出索引选择、扫描行数、Zone Map 剪枝预期 | P1（性能诊断） |
| 物理备份 | ✅ 已有（8.1 清单确认） | 冷备份，打包 SST + 倒排文件 + 元数据 | P0（已完成） |
| 导出 Parquet / CSV / TXT | ✅ 必须支持 | `shanshui-cunji-export` 工具 | P1（分析生态） |
| 导入结构（仅 Schema） | ⚠️ 重新定义 | 导入字段注册表 + 索引定义（不强约束） | P2（高级用户） |
| 导入结构 + 数据 | ✅ 必须支持 | `shanshui-cunji-import`（复用迁移工具） | P1（迁移刚需） |

### 20.2 SHOW PROCESSLIST —— 查看当前运行中的查询 / 操作

- 内部维护全局 **QueryRegistry**（`DashMap<QueryID, QueryContext>`）：每个查询 / 操作（**含后台 Compaction 任务**）进入执行器注册、结束注销；
- 注册信息：QueryID（UUID/自增）、ClientAddr、Command（PUT/GET/SEARCH/DELETE/COMPACTION）、Filter（脱敏）、Plan（PrimaryGet/CompositeScan/Inverted）、StartTime、Duration、State（Executing / Waiting for IO / Computing）、RowsScanned（近似）；
- CLI：`shanshui-cunji admin processlist`；HTTP：`/admin/processlist`；
- **熔断联动**：`KILL QUERY <QueryID>` 向目标查询发送 CancellationToken 强制终止。

### 20.3 SHOW STATUS / 内存与状态监控

无需额外部署 Prometheus 即可快速查看健康状态，按模块暴露指标：

| 模块 | 指标 |
| --- | --- |
| 内存（Jemalloc） | rss_bytes / allocated_bytes / active_bytes / metadata_bytes |
| HotCache | hit_rate / entries / memory_usage_mb |
| BlockCache | hit_rate / memory_usage_mb |
| 倒排字典 | term_count / memory_usage_mb |
| LSM 引擎 | l0_file_count（告警阈值 > 8）/ total_sst_bytes |
| 写入 / 查询 | write_tps / query_qps |

- CLI：`shanshui-cunji admin status`；HTTP：`/admin/status`（JSON）。

### 20.4 EXPLAIN —— 查询执行计划诊断

- `query/optimizer.rs` 提供 `explain(Filter)`：**只做路由推演，不实际执行数据读取**；
- 输出：Access Method（PrimaryGet / CompositeIndex / InvertedIndex / FullScan）、Index Key、Estimated Rows（来自 TermMeta `doc_count`）、Zone Map Pruning 预期（跳过块比例）、Cost（相对代价）、Execution Pool（Network / Compute）、Warning（如"结果集 > 1M，建议加 time_range"）；
- CLI：`shanshui-cunji explain --filter 'status=active AND created_at>2026-08-01'`；HTTP：`/explain?filter=...`。

### 20.5 数据导出：shanshui-cunji-export（Parquet / CSV / TXT / JSON）

- **流式导出管道**：SST 顺序扫描（range 迭代器，无随机 IO）→ Filter（条件筛选）→ Projection（字段映射/脱敏）→ **Sink Adapter 分叉**；每批 `batch_size` 刷一次，**内存恒定**（batch × 单行大小，如 10k × 1KB ≈ 10MB）；
- **ClickHouse（分析引擎首选）**：Parquet（arrow + parquet crate）→ `INSERT ... SELECT FROM file('*.parquet')` 直读，零代码接入；`--dry-run-schema` 生成 MergeTree 建表 DDL；按 `toYYYYMM(created_at)` 分区对齐；阶段 3 可选 JDBC/HTTP INSERT 实时同步（延迟 < 1min）；
- **MySQL（兼容/迁移场景）**：CSV（`--mysql-compatible` 自动转义、处理 NULL/时间格式）+ 自动生成 **LOAD DATA INFILE 配套 SQL**（比逐条 INSERT 快 ~20 倍）；`--jdbc` 直连批量插入（无文件落盘）；`--mysql-max-varchar` 处理 MySQL 65KB 行大小限制，超长字段降级 TEXT；
- **增量导出（数据同步核心）**：`--incremental --checkpoint`（基于 `updated_at` 时间戳游标，首次全量记录最大时间、后续只导增量）；无时间字段场景用 `--range 'docid > X AND docid <= Y'`（DocId 游标）；
- **资源控制**：顺序扫描 + 与 Compaction 共享后台 IO 优先级（默认低于前台读写）、`--rate-limit` 限流防打满网卡、断点续传——对在线业务影响 < 5%；
- **Schema 映射**：

| shanshui-cunji 类型 | ClickHouse 类型 | MySQL 类型 |
| --- | --- | --- |
| DocId (u64) | UInt64 | BIGINT UNSIGNED PRIMARY KEY |
| string | String | VARCHAR(255) 或 TEXT（`--mysql-max-varchar`） |
| i64 / f64 | Int64 / Float64 | BIGINT / DOUBLE 或 DECIMAL(20,6) |
| bool | UInt8 | TINYINT(1) |
| timestamp | DateTime64(3) | DATETIME(3) |
| null | Nullable(T) | NULL |

### 20.6 数据导入：shanshui-cunji-import（结构与数据）

- **核心澄清**：shanshui-cunji 是文档型（弱 Schema），"导入结构" **≠ CREATE TABLE**，而是**导入字段注册表（Field Registry）+ 索引定义（组合索引 / 倒排启用字段）**，且**不强制约束**（未注册字段自动注册）；
- `import-schema`：YAML/JSON 定义字段映射 + 索引（预创建以优化启动性能）；
- `import`：CSV / JSON / Parquet 全量导入，`--id-field` 指定 DocId 列，支持时间字段解析（`--timestamp-format`）与批量写入（`--batch-size`）。

---

## 21. 外部缓存集成（Redis 适配器）

> 定位：Redis 作为 shanshui-cunji 的 **L2 分布式热点读缓存**——**读加速器，不是数据源**。部署与运维细节详见 [redis-integration-guide.md](./redis-integration-guide.md)。

### 21.1 定位与边界（红线）

- **唯一真理源**：所有写入必须先落盘（WAL fsync → MemTable → SST），返回 ACK 后才操作 Redis；
- **单向同步**：仅 shanshui-cunji → Redis（回填 / 失效），**绝不支持 Redis → shanshui-cunji 反向写入**；
- **三条红线**：❌ 禁止只写 Redis 不写 shanshui-cunji；❌ 禁止 Redis 过期后反向回写 shanshui-cunji；❌ 禁止从 Redis 恢复数据（仅可预热）。

### 21.2 交互流程（Cache-Aside + Write-Invalidate）

- **读**：查 Redis（命中 0.1ms 直返）→ 未命中查 shanshui-cunji（HotCache / 磁盘）→ **异步回填** SETEX（`cache_null_values` 防穿透）；
- **写**：先写 shanshui-cunji 成功返回 ACK → **删除 Redis 旧缓存**（DEL，不更新）——删除最安全，下次读自然回填最新值，杜绝不一致；
- **一致性**：默认最终一致（毫秒级收敛）；可选 `write_policy = "double_delete"`（延迟 500ms 二次删除，近似强一致）或版本号 / 时间戳校验。

### 21.3 架构影响（零侵入）

- 单机存储内核（LSM / Compaction / HotCache）**完全无感知**；
- 实现位置：**网关（Proxy）或 SDK 层**新增「外部缓存管理器」模块，不进入数据节点；
- 缓存分层：**L1 Redis**（分布式，跨节点共享）→ **L2 shanshui-cunji HotCache**（进程内）→ **L3 磁盘**；`[cache.external]` 默认关闭（保持单机纯净）。

### 21.4 高可用与极端情况

- **熔断降级**：Redis 超时 / 宕机 → Circuit Breaker 打开直接透传 shanshui-cunji（CLOSED → OPEN → HALF-OPEN 状态机）；
- **防击穿**：热点 key 失效瞬间互斥锁（仅一个请求回源）+ stale-while-revalidate；
- **防雪崩**：TTL 随机抖动（300 + rand(60)）；
- 故障对业务影响仅延迟上升（0.1ms → 0.5~2ms），不雪崩。

### 21.5 落地阶段

- **阶段 2**（分布式成熟后）：基础 Cache-Aside + Write-Invalidate（redis-rs / deadpool-redis，`src/cache/external/`）；
- **阶段 2.5**：预热工具 + 统计监控（`admin cache-warm` / `cache-stats`）；
- **阶段 3**：客户端侧缓存（SDK 本地缓存）、Redis Streams CDC（WAL 变更订阅）；
- MVP 阶段进程内 HotCache 已足够，无需 Redis。

---

## 22. 前沿演进与借鉴：从软硬协同到 AI 索引

> shanshui-cunji 的竞争力在于**"组合"**：LSM 高吞吐写入 + 倒排灵活检索 + 文档模型易用 + 精心设计的缓存/配置/运维体系，组合成一个"趁手"的产品。本章跟踪数据库技术前沿，标注可选择性吸收的方向——均为**远期**，不改变当前务实路线。

### 22.1 四大前沿趋势与借鉴价值

| 趋势 | 现状 | 借鉴与演进 | 阶段 |
| --- | --- | --- | --- |
| **RDMA / CXL**（网络与架构） | PolarDB 用 RDMA 构建"计算-内存-存储"三层解耦；CXL 2.0 分布式内存池（Tigon 吞吐为 RDMA 方案 18.5×） | 当前单机+主从稳健；阶段 3+ 用 RDMA 优化节点间通信；CXL 普及后探索共享内存池，将分布式瓶颈从网络转移到内存访问 | 长期 |
| **持久内存 PMEM** | 接近 DRAM 速度 + 磁盘级持久化（Intel Optane） | 新增存储层级：WAL 放 PMEM 实现"零延迟"持久化；部分 SSTable 索引放 PMEM 加速重启 | 中期（实验） |
| **异构计算**（GPU / IAA） | GCLComp：GPU 卸载 Compaction，Fillrandom P999 尾延迟降 97.27%；Intel IAA 卸载扫描/过滤 | Compaction 成为瓶颈时探索 GPU 加速；利用 CPU 内置加速器卸载扫描过滤 | 中期（评估） |
| **AI 与学习型索引** | RL 调度（PPO 动态调整 B-Tree/LSM 内存分配，切换时间 -83.33%）；HTMdex 学习型索引查询 +1.5~1.76×、内存 -32× | 组合/倒排索引引入 Learned Index；HotCache 热点识别用 AI 模型做精准预测与预热 | 长期 |

### 22.2 行动路线

- **短期（阶段 1.5~2）：立足当下，务实优化**
  - 完成产品化：TTL、Partial Update、FST+Mmap、Admin 命令（见第 4.7/5.4/5.2.4.1/20 章）；
  - 调研 Rust 生态（AuraDB 等）："WAL-time KV 分离"、"RL-driven Compaction" 思路可选择性吸收；
- **中期（阶段 3）：拥抱新硬件**
  - YCSB 标准压测定位真实瓶颈；
  - Compaction 瓶颈 → 小范围 GPU 加速实验；有 PMEM 环境 → WAL 上移实验；评估 Learned Index 用于索引与缓存预热；
- **长期：顺应趋势**
  - CXL 硬件普及后分布式架构重塑机会；AI 赋能智能参数调优与缓存预热。

### 22.3 演进定位

shanshui-cunji 不做"万能数据库"，而是作为**高性能、低成本、易运维的海量文档处理基础设施**，在日志、可观测性、IoT 等特定领域做到极致——这就是极具竞争力的定位。

---

## 附录 D：隐蔽性能陷阱与防御策略（SRE 排障圣经）

| 编号 | 隐蔽瓶颈 | 触发条件 | 表象（症状） | 根治策略（数据结构 / 算法） |
| --- | --- | --- | --- | --- |
| D-1 | 回表随机读放大 | 倒排返回 > 500 个 DocId | P95 5~15ms，远超倒排查询本身 | DocId/(BlockSize) 排序 + 异步批量预取 + 结果集截断（7.3） |
| D-2 | 网卡微突发反压（Burst） | TPS > 5 万，Group Commit 攒批 | 网络丢包重传，延迟抖动 > 10ms | 漏桶 / 令牌桶限流 `network.bytes_per_second_limit` |
| D-3 | Compaction 缓存污染 | 写量 > 50GB/天，触发 Leveled Compaction | BlockCache 命中率骤降 20%~30% | Compaction 完成后主动使旧 FileID 缓存失效（6.7） |
| D-4 | WAL 循环删除锁（Unlink） | 每日切分日志时（UTC 0 点） | 每天固定时间点 P99 写入飙升至 50ms | 延迟删除：重命名 + 后台异步 unlink（4.3） |
| D-5 | Mmap 缺页中断风暴 | 进程刚重启，Page Cache 为空 | 重启后第一个查询 > 200ms | 后台异步 mlock / readahead 预热热点 Term 区域（`mmap.prefetch_ratio`） |
| D-6 | 倒排术语锁竞争（Term Lock） | 极高并发写相同 Term（如 status='active'） | 更新倒排列表原子操作重试，CPU 飙升 | 按 Term Hash 分片细粒度锁（Sharded Lock），而非全局大锁 |

> **D-2 配置**：`network.bytes_per_second_limit = 800MB/s`（预留 20% 给查询流量）；积压超过 10ms 流量时主动拆分 Group Commit Batch，强制平滑输出，杜绝微突发（触发 PFC 反压 → 丢包重传）。
> **D-5 配置**：`mmap.prefetch_ratio = 0.1`——启动后后台异步扫描 FST 前 10% 热点区域（或随机采样 1000 个 Term 位置）执行 mlock / readahead，把"突发缺页"转为"顺序预读"，消除冷启动毛刺。
