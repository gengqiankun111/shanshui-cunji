# 山水存迹数据库（shanshui-cunji）关联查询（JOIN）设计白皮书与使用指导

> 版本：v1.0 | 状态：设计定稿
> 关联文档：[design.md](./design.md)（第 19 章）、[development.md](./development.md)
> 核心理念：**认清边界，用架构设计弥补功能缺失，而非强行实现"伪需求"。**

---

## 1. 现状与边界声明

### 1.1 能力现状

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 单机内多文档关联查询 | ❌ 不支持 | 无 SQL JOIN 语法，无嵌套文档引用解析 |
| 跨分片文档关联 | ❌ 不支持 | 网关禁止跨分片数据重分布计算（design 9.4） |
| 应用层二次查询 | ✅ 支持 | 业务代码自行查多次，SDK 辅助批量查询 |
| 数据导出到分析引擎 | ✅ 支持（规划中） | `shanshui-cunji-export` 导出到 ClickHouse 等 |

### 1.2 为什么不做 JOIN（设计哲学回溯）

design.md 1.2 明确承诺：**放弃跨分片 JOIN，换取水平扩容简单、单点性能保留**。

| 技术原因 | 说明 |
| --- | --- |
| LSM-Tree 随机读代价极大 | JOIN = 一个集合的每一行去另一个集合找匹配行 = 海量随机读；LSM 需查布隆 + 多层 SST，瞬间引爆读放大 |
| 倒排索引只解决单表筛选 | 擅长在同一文档集合内筛选 DocId，无法横向关联另一文档集合（需重分布数据） |
| 分布式是 JOIN 的放大器 | 8 节点 JOIN 需 Shuffle（一个分片数据广播到全部分片），网络开销远超计算收益 |
| 运维确定性优先 | 一个 JOIN 可能消耗全集群 50% CPU，导致正常写入/主键查询大面积超时 |

**边界承诺（书面声明）**：不实现任意多表 JOIN 语法、跨分片 JOIN、嵌套文档自动展开、外键约束 / 级联操作。

---

## 2. 需求拆解：用户"想要 JOIN"的真实需求

| 真实需求 | 典型场景 | 是否必须 JOIN |
| --- | --- | --- |
| A. 文档扩展 / 补全 | 订单文档只有 user_id，需补全用户名、等级 | ❌ 写入时预连接（§4.2） |
| B. 两表交集筛选 | 找"购买了 A 商品"且"来自北京"的用户 | ❌ 两次查询 + 内存交集（§4.1） |
| C. 多表聚合统计 | 按用户维度统计所有订单总额 | ⚠️ 轻量→物化视图（§4.3）；重型→导出（§4.4） |
| D. 复杂关联分析 | 画像 + 行为 + 订单三表漏斗分析 | ✅ 属 BI/OLAP 范畴，shanshui-cunji 不做 |

**本质结论**：95% 的"需要 JOIN"实际是"需要数据关联后的结果"，而非"需要 JOIN 语法本身"。我们不需要实现 JOIN 算法，只需提供"数据关联"的替代路径。

---

## 3. 支持与不支持清单

### 3.1 必须支持（列入开发路线图）

| 需求 | 优先级 | 实现方式 | 落地阶段 |
| --- | --- | --- | --- |
| 单文档内多字段组合查询 | P0（已有） | 倒排索引 + 组合索引 | ✅ 已完成 |
| 批量 DocId 回表取文档 | P0（已有） | 批量预取 + 块排序优化 | ✅ 已完成 |
| 应用层二次查询 SDK 辅助 | P1 | SDK 提供 `queryAndJoin()` 辅助方法 | 阶段 1.5 |
| 写入时"预连接"（多文档源合一） | P1 | 写入时将关联数据展开到单文档（Enrich） | 阶段 1.5 |
| 物化视图（单表预聚合） | P2 | 后台任务定期构建聚合结果文档 | 阶段 2 |
| 数据导出到 OLAP 引擎 | P2 | `shanshui-cunji-export` → ClickHouse / MySQL | 阶段 2 |

### 3.2 明确不支持

| 能力 | 原因 | 替代方案 |
| --- | --- | --- |
| 任意多表 JOIN 语法 | 违背核心设计哲学 | 导数据到 ClickHouse |
| 跨分片 JOIN | 网络 Shuffle 不可接受 | 业务层分片路由规避 |
| 嵌套文档自动展开 | 增加存储与索引复杂性 | 写入时主动展平 |
| 外键约束 / 级联操作 | 不是文档数据库的职责 | 应用层维护 |

### 3.3 用户预期管理

> shanshui-cunji 是**文档检索数据库**，不是关系型数据库，**不提供 SQL JOIN 能力**。但为常见的"数据关联"场景提供了多种高效替代方案（见 §4）。若业务需要复杂多表关联分析，建议将数据导出到专业 OLAP 引擎（如 ClickHouse），shanshui-cunji 提供一键导出工具。

---

## 4. 四种关联替代方案

### 4.1 策略一：应用层"二次查询 + 内存合并"（最推荐，开箱即用）

**适用场景**：两张表通过一个外键关联，结果集不大（< 10 万条）。

**原理**：

```
1. 先在 A 表用倒排查出符合条件的 DocId 列表 [1, 2, 3, ...]
2. 批量回表取 A 表完整文档（得到 user_id 列表）
3. 用 user_id 列表去 B 表做第二次批量查询（批量主键点查）
4. 在应用内存中做 Hash 匹配（左连接 / 内连接）
```

**性能数据（单机 1.5 亿数据，结果集 10 万条）：**

| 步骤 | 耗时 | 说明 |
| --- | --- | --- |
| A 表倒排筛选（10 万条） | ~2ms | 内存字典定位 + Bitmap 解压 |
| A 表批量回表（10 万条） | ~5ms | 批量预取 + 块排序优化（design 7.3） |
| B 表批量点查（10 万个 user_id） | ~8ms | 全部走主键，可能命中 HotCache |
| 内存 Hash 匹配 | ~1ms | 纯 CPU 操作 |
| **总计** | **~16ms** | 用户端可接受 |

**SDK 使用（Java / Python / Go）：**

```java
// shanshui-cunji SDK 提供开箱即用的 JOIN 辅助
List<JoinedDoc> results = client.join()
    .from("orders")                // 主表
    .filter("status", "active")    // 主表筛选
    .joinOn("user_id")             // 关联键
    .with("users")                 // 从表
    .type(JoinType.LEFT)           // LEFT / INNER / RIGHT
    .limit(1000)
    .execute();
```

### 4.2 策略二：写入时"预连接"（Enrich，最优方案）

**适用场景**：两张表的关联关系固定，且关联数据在写入时已知。

**原理**：写入订单文档时，主动将用户信息（用户名、等级、注册时间）展开到订单文档：

```json
// 写入前：只存 user_id
{"docid": 1001, "order_id": "O001", "user_id": 123, "amount": 99}

// 写入时预连接后（业务层处理）
{"docid": 1001, "order_id": "O001", "user_id": 123,
 "user_name": "张三", "user_level": "VIP", "amount": 99}
```

**收益**：查询订单无需二次查用户表，单文档直接返回全部信息；所有索引（倒排/组合）只建在订单文档上，查询效率最高。
**代价**：用户信息更新时需批量更新所有关联订单（用后台异步任务）。

**写入路径增强（SDK Enrich 回调）：**

```rust
// 写入时，业务层通过 SDK 的 "enrich" 回调
client.put(doc).with_enrich(|doc| {
    // 从 Redis/MySQL 中获取关联数据，展开到文档
    let user = user_cache.get(doc.user_id);
    doc.user_name = user.name;
    doc.user_level = user.level;
    doc
});
```

**写入路径（design 4.1 扩展）：**

```
写请求 → 业务层 Enrich（可选）→ WAL → MemTable → ...
                │
                ▼
        Enrich 回调：查询关联数据（Redis/MySQL/HTTP/本地缓存）并展开到文档
```

- Enrich 回调在**网络层接收后、WAL 写入前**执行；
- Enrich 失败（关联数据不可达）：可配置为"拒绝写入"或"降级写入"；
- Enrich 数据源可配置：Redis、MySQL、HTTP API、本地缓存。

### 4.3 策略三：物化视图（后台预聚合，离线场景）

**适用场景**：固定维度的聚合统计（如"按用户统计月订单总额"），不需要实时精确。

**原理**：后台定期（如每小时）扫描新写入订单，按 user_id 分组聚合，生成结果文档写入独立文档集；运营查询直接查结果集（单表倒排，毫秒返回）。

```
订单表（实时写入）─── 后台定时任务 ───→ 物化视图表（聚合结果）
                                          ↑
                                    运营查询直接查这里
```

**与 MongoDB / ES 物化视图差异：**

| 特性 | MongoDB 物化视图 | shanshui-cunji 物化视图（设计） |
| --- | --- | --- |
| 实时性 | 增量更新（$merge） | 定时全量 / 增量重建（阶段 2） |
| 存储位置 | 同一集合 | 独立文档集（利用现有索引） |
| 查询方式 | 直接查询 | 倒排 / 组合索引（毫秒级） |

### 4.4 策略四：数据导出到 OLAP 引擎（终极方案）

**适用场景**：复杂分析、多表 JOIN、全量扫描、BI 报表。

**数据流**：

```
shanshui-cunji（主存储）─── 定时导出 ───→ Parquet 文件 ───→ ClickHouse（分析引擎）
                                                         ↑
                                                   BI 工具查询
```

- 导出命令：`shanshui-cunji-export`，支持 Parquet / CSV / SQL INSERT 三种格式；
- **增量导出**：基于 `updated_at` 时间戳字段，支持断点续传，避免重复导出。

---

## 5. 模块与接口设计

### 5.1 新增模块清单

| 模块 | 位置 | 职责 | 落地阶段 |
| --- | --- | --- | --- |
| JOIN 执行器（应用层辅助） | `sdk/join.rs` | 二次查询 + 内存合并的标准化实现，支持 Left / Inner / Right Join | 阶段 1.5 |
| 数据导出服务 | `tools/shanshui-cunji-export/` | 将文档集导出为 Parquet / CSV / SQL INSERT | 阶段 2 |
| 物化视图调度器 | `engine/mv_scheduler.rs` | 定时（Cron）或触发式执行聚合任务，写入结果文档 | 阶段 2 |
| 关联字段索引提示 | `query/optimizer.rs` | 识别 JOIN 场景，建议使用主键点查或批量预取（不改变执行） | 阶段 1.5 |

### 5.2 查询路径增强（JOIN 计划节点）

```rust
// 查询执行器新增 Join 计划节点
enum QueryPlan {
    // ... 现有计划 ...
    Join {
        left_plan: Box<QueryPlan>,   // 主表查询
        right_plan: Box<QueryPlan>,  // 从表查询（通常是主键点查）
        join_type: JoinType,         // Left / Inner / Right
        join_key: String,
        limit: usize,
    }
}
```

**执行策略（由优化器选择）：**

| 情况 | 策略 |
| --- | --- |
| 左表集小 | 先查左表 → 批量查右表（推荐） |
| 右表集小 | 先查右表 → 批量查左表 |
| 两表都大 | **拒绝执行**，返回错误提示（引导用户用导出 + OLAP） |

### 5.3 网关层 JOIN 策略（分布式下）

| 策略 | 说明 | 是否支持 |
| --- | --- | --- |
| 单分片内 JOIN | 两个文档集在同一分片内（DocId 哈希同源），本地执行 | ✅ 支持（同单机） |
| 跨分片 JOIN | 左表分片 0、右表分片 1，需重分布数据 | ❌ 不支持，返回错误 |
| 广播小表 JOIN | 右表极小（< 100 条），广播到全部分片 | ⚠️ 可选（阶段 3），见 5.4 |

### 5.4 "小表广播 JOIN"的特殊设计（可选，阶段 3）

**适用场景**：右表极小（如配置表 < 100 条），且频繁关联查询。

**实现**：

1. 网关识别右表为"小表"（`join.broadcast_threshold`，默认 100 行）；
2. 网关先将右表全量查出（单分片操作）；
3. 将右表数据随查询请求广播到所有分片；
4. 每个分片本地执行 JOIN（左表 + 广播的右表）；
5. 各分片返回结果，网关合并。

**风险控制**：

- 小表判定必须严格（< 100 条），防止意外广播大表；
- 广播数据量上限 `join.broadcast_max_bytes`（默认 1MB）；
- **默认关闭**，需显式开启 `join.broadcast_enabled = true`。

---

## 6. 性能影响评估

### 6.1 二次查询 JOIN（策略一）

| 左表返回行数 | 右表点查数 | 总耗时 | 说明 |
| --- | --- | --- | --- |
| 100 条 | 100 次 | 1.5ms | 全部命中 HotCache |
| 1 万条 | 1 万次 | 6ms | 批量预取 + 块排序优化 |
| 10 万条 | 10 万次 | 16ms | IO 并行化 |
| 100 万条 | 100 万次 | ~150ms | 内存占用 ~500MB，建议改用物化视图或导出 |

### 6.2 预连接（策略二）

| 指标 | 值 | 说明 |
| --- | --- | --- |
| 单次写入开销 | +0.1~0.3ms（Enrich 回调） | 关联数据在 Redis 时 |
| 查询开销 | 0 增加 | 数据已展开到单文档 |
| 更新开销 | 高（需批量回写订单） | 适用于低频更新的维度表 |

### 6.3 物化视图（策略三）

| 指标 | 值 |
| --- | --- |
| 聚合任务耗时 | 每小时 100 万条新订单 → ~5s（纯扫描 + 分组聚合） |
| 查询延迟 | < 1ms（直接查物化视图倒排索引） |
| 存储增量 | 聚合结果文档量 = 用户数（如 1000 万行 × 200B = 2GB），可接受 |

---

## 7. 分阶段落地计划

| 阶段 | 内容 | 交付物 |
| --- | --- | --- |
| 阶段 1.5 | ① SDK 二次查询 JOIN 辅助（`queryAndJoin`）；② 写入 Enrich 回调 | SDK 文档 + 代码示例 |
| 阶段 2 | ① 物化视图调度器（Cron 触发）；② 数据导出工具（Parquet / CSV）；③ JOIN 计划节点 + 本地执行 | 工具 + 设计文档 |
| 阶段 3 | ① 小表广播 JOIN（可选）；② 导出到 ClickHouse 直连 | 性能测试报告 |

---

## 8. 详细使用指导

### 8.1 SDK：二次查询 JOIN（queryAndJoin）

```java
// 主表筛选 + 关联从表 + 内存合并，一次调用
List<JoinedDoc> result = client.join()
    .from("orders").filter("status", "active")
    .joinOn("user_id").with("users")
    .type(JoinType.LEFT).limit(1000)
    .execute();

// 底层自动执行：倒排筛选 → 批量回表 → 批量主键点查 → 内存 Hash 匹配
```

### 8.2 SDK：写入 Enrich（预连接）

```java
client.put(doc)
    .with_enrich(d -> {
        User u = userCache.get(d.field("user_id"));
        d.set("user_name", u.name);
        d.set("user_level", u.level);
        return d;
    })
    .execute();
```

### 8.3 CLI：数据导出（shanshui-cunji-export）

```bash
# 全量导出为 Parquet（供 ClickHouse/Spark 分析）
shanshui-cunji-export --collection orders --format parquet --out /data/export/

# 增量导出（按 updated_at 断点续传）
shanshui-cunji-export --collection orders --format parquet \
    --since "2026-08-01T00:00:00Z" --resume
```

### 8.4 相关配置项

| 配置项 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `join.broadcast_enabled` | bool | false | 小表广播 JOIN 总开关（阶段 3） |
| `join.broadcast_threshold` | u32 | 100 | 右表行数低于此值才允许广播 |
| `join.broadcast_max_bytes` | u64 | 1MB | 广播数据量上限 |
| `join.max_rows` | u64 | 1_000_000 | 二次查询 JOIN 结果集上限，超限熔断 |
| `enrich.fail_policy` | enum | "reject" | Enrich 失败策略："reject"（拒绝写入）/ "degrade"（降级写入） |
| `enrich.source` | enum | "redis" | 关联数据源："redis" / "mysql" / "http" / "local" |

### 8.5 最佳实践（推荐 / 禁止）

**✅ 推荐做法：**

1. **单表查询能搞定的绝不用 JOIN**：利用倒排 + 组合索引，95% 的筛选需求单表可解；
2. **需要补全信息的用"预连接"**：写入时展开关联数据，查询一次返回；
3. **需要两表交集的用"二次查询"**：先查主表得 ID 列表，再批量查从表，SDK 自动完成；
4. **需要固定维度聚合的用"物化视图"**：后台定时聚合，运营查询直接查结果表。

**❌ 绝对禁止：**

1. **跨分片 JOIN**：网关直接拒绝，返回 `Query Not Supported`；
2. **大表 JOIN 大表**：超过 `join.max_rows`（默认 100 万）的关联查询被熔断。

**📊 决策树：**

```
你的需求是什么？
│
├─ 单表任意字段筛选 → ✅ 直接用 search()，毫秒响应
│
├─ 两表关联查补全信息 → ✅ 写入时预连接 / 二次查询 SDK
│
├─ 固定维度聚合统计 → ✅ 物化视图（后台定时构建）
│
└─ 复杂多表 JOIN / 分析报表 → ⚠️ 导出到 ClickHouse（提供一键工具）
```

---

## 9. 总结与边界声明

| 维度 | 结论 |
| --- | --- |
| 原生 JOIN 支持 | ❌ 不实现（违背设计哲学） |
| 应用层二次查询 | ✅ 支持，SDK 提供标准化辅助 |
| 写入时预连接 | ✅ 支持，通过 Enrich 回调展开关联数据 |
| 物化视图 | ✅ 支持（阶段 2），定时聚合，结果存入文档 |
| 数据导出到 OLAP | ✅ 支持（阶段 2），一键导出 Parquet / CSV |
| 小表广播 JOIN | ⚠️ 可选（阶段 3），默认关闭，需显式开启 |
| 跨分片 JOIN | ❌ 网关层拦截，返回明确错误 |

**一句话概括**：

> shanshui-cunji 不提供 JOIN 语法，但提供了 **4 种高效的"数据关联"替代方案**，覆盖 95% 的真实业务需求。需要复杂关联分析的，请导出到专业 OLAP 引擎——这正是现代云原生架构"存储与计算分离"的正确姿势。
